# Promptfoo Integration Notes

This directory is reserved for Promptfoo configs/assertions once the local harness baseline is stable.

The current iteration uses Python scripts in `eval/readme/scripts/` as the canonical local loop.
