# Workflow Variant Staging

Store experimental external workflow profiles and prompt templates here.

Suggested convention:

- `workflow/<variant_id>.yaml`
- `prompts/<variant_id>/<turn_id>.md`

Promote only validated variants into runtime defaults.
