# TODO

Deferred directions that are real but must not displace the core goal: theory elevation, meaning the hand-lowered docs stewardship image becomes durable installed theory and a second dissimilar expression proves runtime agnosticism, per the [Theory Elevation Program](../plan/integration/theory_elevation_program.md). Each entry names the direction and its trigger; sequencing and evidence belong under `design/plan`.

## Cost Record

Durable outcomes should carry their actual cost — tokens, wall time, provider and model identity, spend — alongside the attribution lineage already stamps. Provider responses parse usage today and nothing persists it; estimates have vocabulary in the shared language while actuals have no durable home. Raise this when a real consumer exists, the first being the multiplier comparison of quality per cost across providers. The multiplier program's other dependencies are now met, so that consumer is one Ollama validation away.

## Failure Signal Expansion

The canonical semantics are recorded in [Failure Signal](execution/failure_signal.md) and the bounded first slice is planned in [Gate Signal First Slice](../plan/integration/gate_signal_first_slice.md). What remains deferred here is the expansion: settling gate vocabulary and ownership across the codebase and design corpus, and any failure-signal channel into belief richer than the terminal outcome mapping. Its trigger — the parity workstream reaching its Strategy pause — has fired; raise it whenever it next competes for capacity against the elevation steps.
