# Repository relationships and README claims

Slice `repository-claims-v1`. Lifecycle: delivered. Readiness: technically qualified for the bounded claim journey. Baselines: Meld `15924ab1`, Codebase Semantics `59338cf`, README `93b3960`, Eval `1d4a9d7`. The user explicitly authorizes finishing the event-up experiment as designed, retaining its architecture and deferring broader implications of mathematical semantic density. Standing checkpoint commit and push authority remains effective.

## Observable contract

Complete the remaining bounded path on isolated Gmail Operator: committed syntax Events → supported native repository relationships → README-owned claim selection → Event-authored obligations → native assessment, Strategy decomposition, draft and publish → independent observation and reconciliation. The source checkout is read-only. No source interpretation, claims or publication are supplied by the harness as accepted results.

Codebase Semantics interprets declarations across the captured repository. Python declarations, explicit import candidates, route decorators and command registration calls, TOML package metadata and script entries, and YAML resource declarations supply concrete language-adequacy examples. Every rule names its observational limits. Local module candidates are not assertions of Python runtime resolution. Route declarations are not claims about live deployment, authorization or execution. Unrecognized, malformed and ambiguous forms remain explicit coverage or unresolved products.

README selects a small declared family of useful claims from native relationships and derives obligations without manually paired true/false sentences or per-claim source selectors. Selected claims carry native source support. Native Strategy remains the sole planner; the existing draft and publish capabilities remain the sole writer path. The experiment qualifies those managed claims and their prose correspondence, not arbitrary whole-document behavioral completeness.

## Responsibility assessment

The system landscape includes source observation, Events, native Graph and language, epistemic Curation and Belief, Agent and Strategy, Execution, providers, README and Eval. All participate on the existing path except release and unrelated tooling, which have no integration. Provider selection and release configuration are retained. No new runtime participant, service, store, coordinator, primitive language construct or direct Graph writer is selected.

| Responsibility | Mode and owner | Existing route and retained state | Required proof |
| --- | --- | --- | --- |
| Repository interpretation | new local behavior in Codebase Semantics | Committed file products consumed by the existing representation participant, shared language/publication mapper and outbox | Native local and cross-file query, exact Event and syntax support, explicit unresolved cases, changed-basis replacement |
| Syntax preservation | retain in Codebase Semantics | Opt-in faithful representation and canonical Events | Faithful predecessor remains a regression contract |
| Claim selection and materiality | extend in README | Native semantic input callback, existing Contribution Event admission and assessment | Automatically discovered claim set, unchanged semantic claims quiet on irrelevant edits, source changes revise selected claims |
| Prose correspondence | extend in README | Existing domain verification and native draft/publish capability path | Missing, changed and removed selected claims produce observed mismatch and native correction |
| Graph, Events, language | retain in Meld | Owner publication Events, complete scope replacement, typed properties and relations, bounded traversal | Real public command evidence; only demonstrated generic gaps permit thin corrections |
| Curation, Agent, Strategy, Execution | retain in Meld | Existing observation → evidence → maintained condition → native recursive plan → Task Network | Observable goals, selected methods and actual task artifacts, followed by independent confirmation |
| Experiment control | extend external harness | Eval capture and public Meld commands | Isolated checkout, exact binary identities, restart, retained errors and bounded convergence |

The prior Boolean experiment remains a separately selected regression contract. The new repository mode extends the canonical owners rather than installing a competing planner or writer. Domain meaning remains outside Meld core under Runtime Invariants. Cargo builds are routine; release publication is excluded.

## Proof and limits

Start with deterministic semantic construction and native inspection. Measure selected and excluded families, support and ambiguity. Then exercise README claim discovery and native maintenance with Luna low using the existing provider binding. An unchanged claim set must survive irrelevant source edits without invented factual churn. A material source change, deletion and restoration must eventually reconcile, preserving prior evidence. Stale intermediate drafts remain legitimate; no universal freshness barrier is introduced.

This initiative remains exploratory. A need for new stores, competing semantic authorities or major admission redesign triggers the existing architectural circuit breaker. The generic owner-crash recovery gap remains open. The complete-snapshot publication protocol and parse caching are not redesigned in this slice. Meaning-mode selection may narrow the materialized native view while source Events and the separately selected faithful mode retain the complete syntax product. No mathematical semantic-density metric or general per-PDS converter architecture is introduced.

Logical, source-quality and gate self-review evaluated the exact candidate in order. If applied, this commit connects repository-derived claims to the existing native README planning and maintenance loop. Completion requires actual command evidence and checkpoint publication; broader README quality and arbitrary semantic inference remain outside the bounded claim contract.

## Native findings during delivery

The first owner-authored contribution used the external input namespace and was refused by the native Event grant. The correction declares the Contribution event type as an observation publication for the README owner, using its native session and scope. The existing intake accepts that owner-authored Event as well as the established external contribution route. The pending envelope is retained in the existing intake state until durable append is acknowledged. No new ingress contract or runtime permission bypass is needed.

The initial public Graph request arrived before a canonical projection position existed. The harness now recognizes that specific not-yet-ready response and waits for a complete native cut, retaining all other errors. The native language query resolves three configuration-module import candidates from the CLI file. The corrected source-v2 candidate exposes 1,833 propositions to that bounded query. Full syntax remains retained in source Events and the separately selected faithful native view.

The generated README exposed a contradictory source assumption: v1 TOML string nodes can contain quote children without content children. Filling unobserved spans with spaces invented values, and the table verifier correctly matched those incorrect upstream values. The candidate is rejected despite matching-table success. Source capture v2 now retains exact string-node text alongside child structure; the semantic converter requires complete lexical coverage and marks unsupported literals unresolved. The v1 reader remains for retained Event history, and source scans accept a v1 predecessor while emitting a v2 successor. This is an extension of the same Event product shape with an explicit extractor revision.

Measured full-tree projection cost made the first native draft/publish cycle take several minutes. Meaning-enabled publication now includes declarations, per-file coverage and only their supporting syntax nodes. The separate faithful mode remains unchanged in scope; complete source trees remain in Events. Both modes use the same writer and complete-scope replacement contract. The first bounded meaning view contains 453 objects and 577 relations in a roughly 1.3 MB envelope. This is a projection selection correction in the external domain, not a new runtime index or incremental publication protocol.

Consumer review found that a parse failure must not masquerade as an empty declaration catalog. Coverage objects now accompany the selected native families. Unavailable relevant source withholds README judgment; observed absence permits removal. This is consumer-owned interpretation of source coverage, not a global action freshness rule. The harness includes malformed-source recovery alongside unsupported literal recovery.

The declaration rules are bounded to the observed forms. Quoted or aliased YAML structures, dynamic Python calls, relative import resolution and general TOML layouts are not qualified. Syntax retention does not imply that every unsupported semantic form is recognized as a claim. This slice supplies coverage and explicit unresolved values for the exercised rules; broader rule completeness remains a domain-development question.

## Technical closeout

The [consumer closeout](../../../../../meld-readme/evidence/repository-claims-v1/CLOSEOUT.md) records the 35-check native maintenance and restart journey, completed cross-file reconciliation and corrected identity oracle over the exact captured native products. The original relationship report remains failed only for comparing unordered publication arrays; the corrected comparison preserves declaration-name-to-object associations. Three Luna-low calls perform document drafts, and zero calls are needed for restart or unselected relationship changes. Producer regressions pass 137 repository checks, 89 bounded sensor checks and retained-v1 conversion parity. The actual Gmail Operator checkout is unchanged.

No core source or language changes are part of this slice. This qualifies declaration reference sections and the native README planning path, not arbitrary prose entailment or production maturity. The producer still carries a predecessor `semantic_snapshot` currentness goal that repository mode does not supply. Sensory publication continues and the README Agent completes, but producer Agent directive reconciliation is unqualified. Debug-build contribution-to-acceptance times grow from 110 to 205 to 417 seconds, exceeding provider latency substantially. These domain and cost findings remain explicit; no new core authority or broader mathematical design is selected to explain them away.
