# World Model Reconciliation Outcome Audit

Date: 2026-09-06

Status: initial source audit complete; implementation underway; end-to-end reconciliation remains incomplete

## Authority and evidence

The program owner explicitly withdrew the binding status of the delivery program and authorized audit of VC-01 through VC-06 followed by implementation of the complete rearchitecture. Previous slice exclusions and advancement permissions cannot narrow this mission. This account is an evidence and work record, not a substitute definition of the intended system.

The source baseline is `85f4b85de1f68f76f540b8c1145419bdc2111597` plus the uncommitted VC-06 candidate. A binary diff and untracked-file archive were preserved outside the repository at `/tmp/meld-reconciliation-audit-20260906` before this work. The prior candidate has useful code and proof, but is not accepted as semantically complete.

The discovery throughline `1c08298e-f495-467d-9fd9-6c9c0e022f53` was recovered read-only from local conversation storage. Key original user decisions are dated August 19 through August 23: one opt-in traversal substrate; Strategy as the only planning workflow; Curation as separate epistemic authorship; heterogeneous Plans whose Tasks are independently complete; independent compatible admissions sharing Execution work; PDS as product data for the same runtime; Startup as a minimal proof that work returns through epistemics to Agent.

The intended baseline is the [evergreen world-model design](../../../cognitive_architecture/world_model/README.md), the original detailed designs, and the [observable outcome matrix](detailed_design/integrated_product_outcome_evidence_matrix.md). Outcome identifiers are useful trace labels. Their gate allocation and historical acceptance do not prove implementation. The [Runtime Invariants](../../../../governance/runtime_invariants.md) preserve canonical ownership and removal of superseded authority.

The initial audit uses a breadth-first source sweep and one level of affected-domain decomposition. It is bounded to the direct runtime path and its owner contracts. Source findings are distinguished from independently executed proof. Uninspected recovery or product combinations remain unproved, even where earlier receipts report passing tests.

## Required behavior

Meld must turn complete owner observations into connected, owner-qualified knowledge that an Agent can use to judge its directive. Strategy constructs the remaining epistemic and executable work against that knowledge. Agent authorizes eligible products, Curation authors epistemic results, and Execution realizes complete Tasks. Owners observe effects and admit returned evidence. Agent then maintains, satisfies, holds, or reconstructs its work. Changed knowledge must change future decisions while preserving completed history. A correctly maintained condition must not force executable work.

Simplification means one authority per responsibility, connected through existing owner products and stores. It does not mean replacing rich owner evidence with generic status, adding a second coordinator, or forcing every product into the currently demonstrated Curation-before-Task trace.

## VC-01 through VC-06 baseline findings

This table records the initial audit baseline. Implemented corrections and current remaining work follow below.

| Source area | Behavior present in source | Outcome still missing or unproved |
| --- | --- | --- |
| VC-01 publication and traversal | Workspace scan creates typed publication candidates, including unchanged scans. Graph retains owner publications and occurrence identity. Queries expose completeness, lag, bounded traversal, and owner revisions. | Docs source claims and README correctness observations are not demonstrated by workspace existence evidence. Security products must retain their own semantics. Full product coverage is partial. |
| VC-02 Curation and settlement | One standing actor selects planned intake or standing work and uses the same bounded execution and durable publication path. Replay tests cover acceptance, terminality, and pending publication. Configured Belief has a separate evidence owner. | Product-specific claim coverage, post-execution confirmation, and resulting Goal decisions are not established by the prerequisite trace. Agent currently accepts Curation terminality as its one epistemic milestone shape. |
| VC-03 Planner, Strategy, Agent | Planner builds exact limited cuts or refuses. Strategy searches pure executable compositions, wraps a Task and optional epistemic prerequisite, and has a pure successor helper. Agent durably records decisions, authorizations, and owner receipts. | The verifier requires one Task and at most one prerequisite operation. Production never calls successor search. Root constructs a proposed breach Goal before native Agent judgment. No-action disposition and generic milestone progression are absent from this driver. |
| VC-04 Execution | Exact authorized Tasks enter durable admission, lower into the existing Task Network, execute production Capabilities, and return operational outcomes. | Lowering explicitly creates distinct admission regions. Compatible sharing and separate discharge are absent. Agent's implemented return consumer recognizes Execution terminality; owner observation and Goal satisfaction remain outside the demonstrated route. |
| VC-05 product preparation | PDS compiles declarations, owner installation receipts, Agent genesis, Belief subscription acceptance, topology, and inert preparation. Runtime hydrates the exact prepared product. | Preparation does not demonstrate correct directive evaluation or full product behavior. Runtime assembly still creates Agent semantic inputs, including an unconditional breach proposal. All installed product behaviors need composed validation. |
| VC-06 lifecycle | One assignment aggregate, generation and incarnation fencing, atomic publication and admission, production liveness projection, and retirement mechanics exist. | Root still derives owner readiness and safe-point meaning. Active resolvers recognize namespaces without binding exact resources. Passing transition tests does not prove native evidence authorship or viable wakes. |

Primary source anchors: [workspace scan](../../../../src/workspace/scan.rs), [Graph query](../../../../crates/meld-world-model/src/world_state/graph/query.rs), [Curation actor](../../../../crates/meld-world-model/src/curation/actor.rs), [Curation recovery tests](../../../../crates/meld-world-model/src/curation/tests.rs), [Planner assembly](../../../../crates/meld-world-model/src/planner/projection.rs), [Strategy verifier](../../../../crates/meld-world-model/src/strategy/verification.rs), [Agent driver](../../../../crates/meld-world-model/src/agent/actor.rs), [Task lowering](../../../../crates/meld-execution/src/task_admission/lowering.rs), [product preparation](../../../../src/theory/product.rs), [root assembly](../../../../src/runtime/assembly.rs), and [lifecycle projection](../../../../src/runtime/lifecycle/store.rs).

## Domain sweep

The universe comes from root `src/lib.rs`, the four crate roots, and their public domain contracts. Root forwarding modules and crate-owned implementations are assessed once under the semantic owner. Utility modules are explicitly accounted for together because they introduce no additional reconciliation owner.

| Domain | Needed integration | Current relationship and completeness | Follow-up |
| --- | --- | --- | --- |
| workspace, tree, branches, heads | publish | Real scan and authoritative source positions; partial product return | Retain scan authority; connect complete observations after effects |
| docs | own, publish | Executable claim workflow and installed theory; partial observation product | Observe existing claims before work and re-observe after work |
| dependency security | own, publish | Inventory, advisory, assessment, verification owners exist; native shared route incomplete | Connect dissimilar product through the same reconciliation owners |
| Events | own | Neutral durable append and replay on the path | Reuse unchanged; prove independent consumer positions |
| Graph and Traversal | own | Owner publication projection and bounded cuts exist | Reuse structural authority; remove reliance on generic Event presence as semantic proof |
| Belief and evidence ingestion | own | Configured evidence routes and subscriptions exist; full feedback partial | Verify changed evidence reaches Agent decisions and satisfaction |
| Causation and Regime | publish | Planner can name these source classes; no separate native top-level owners in current crate | Keep limited cuts explicit; do not claim full epistemic context from absent sources |
| Planner | own | Exact cut assembly, source validation, derived view | Reuse cut authority for currentness and successors |
| Curation | own | Shared standing and planned operation path | Support product-required confirmation and exact terminal meaning |
| Strategy | own | Pure search and successor helpers; narrow Plan grammar | Generalize product graph and remove one-Task restriction where real behavior requires it |
| Agent | own | Durable reconciliation store and driver; incomplete decision loop | Native directive judgment, successor adoption, milestone progression, Goal disposition |
| Execution, task admission, Task Network | own | Real complete-Task admission and dispatch; partial coherence | Compatible sharing with independent discharge; retain execution-only meaning |
| Capability and provider | own | Installed contracts, bindings, executors | Reuse existing execution seams; add reusable nonce publication when required |
| Meld Language | publish | Shared nouns, propositions, operators, Tasks | Keep permissive; semantic Plan policy stays in Strategy |
| theory and initialization | own | Product compilation and inert preparation | Reuse exact owner installation; remove remaining semantic composition shortcuts |
| runtime | own, adapter | Structural lifecycle plus foreign semantic construction | Move semantic evidence into owners and bind exact wake resources |
| CLI, API, serve, control | adapter, observe | Runtime and owner presentation | Expose exact missing progress without creating decision authority |
| harness | observe | Real composed proofs plus fixtures | Exercise production entrypoints; do not become product orchestration |
| workflow | none | Explicitly legacy planning surface | Inventory and retire remaining superseded authority when real callers migrate |
| context, prompt_context, views, merkle_traversal, session | consume or adapter | Existing workspace and execution context support | Reuse for real consumers; no independent world-model planning authority |
| config, store, concurrency, metadata, ignore, telemetry, logging, errors, types, compatibility | none | Supporting storage and process facilities | No direct semantic change indicated; preserve external runtime-state roots |

The initial affected set contains each non-`none` row. Runtime participation is not a requirement to edit every such domain. Workflow retirement remains an explicit replacement concern even though it is not an intended participant.

## Affected-domain decomposition and boundaries

| Concern | Owner | Required relationship | Change posture |
| --- | --- | --- | --- |
| observed existence, claims, completeness | workspace, docs, security | Producer-issued observations before and after work | Extend existing owners |
| transport, replay, structural discovery | Events, Graph, Traversal | Immutable owner products and independent durable cursors | Reuse existing authority |
| epistemic authorship and admission | Curation, Belief | Expected state, comparison, configured evidence, changed revisions | Extend existing product behavior |
| source consistency | Planner | Same exact assembly for initial construction and currentness | Reuse existing assembly |
| causal product construction | Strategy | Heterogeneous dependency graph, no-action closure, successors | Extend pure construction and verification |
| directive and Goal judgment | Agent | Decide mismatch and satisfaction from admitted evidence | Move root construction and extend native decisions |
| Plan progression | Agent | Current Plan selection, product milestones, successor adoption, retained history | Replace first-authorization control path |
| executable realization | Execution | Complete Tasks, compatible work, independent outcomes | Extend existing admission and Task Network |
| product installation | theory and native installation owners | Exact prepared product and installed directives | Reuse; validate composed products |
| readiness, waiting, safe points | each native participant | Owner derives meaning from its bound state | Move root semantic construction and remove predecessor helpers |
| structural activation | runtime | Bind and aggregate native evidence for exact instances | Retain aggregate; reduce root interpretation |
| inspection | native queries, CLI and harness adapters | Find missing owner position without inventing truth | Extend projections and production proofs |

## Outcome coverage and remaining work

Every applicable catalog outcome is retained below. Evidence of a necessary substep is never reported as the complete outcome.

| Outcomes | Initial source assessment | Concrete remaining obligation |
| --- | --- | --- |
| O01 | Substantial preparation implementation and local proofs | Revalidate installed product selection through runtime |
| O02, O19, O20, O22 | Lifecycle structure present; owner-evidence defects remain | Native semantic readiness and safe points, exact bindings, recovery and retirement proof |
| O03, O04 | Workspace observation exists; full Docs no-action route missing | Observe correct README, admit correctness, maintained-condition disposition without Task |
| O05 | Root creates breach proposal unconditionally | Agent judges mismatch from complete admitted knowledge |
| O06, O07, O08, O23 | One prerequisite-Curation and one-Task trace | Heterogeneous product dependencies and exact visibility or Belief milestones |
| O09 | Production admission and executable route exist | Revalidate alongside revised Agent progression and shared admission |
| O10, O11, O12, O13 | Operational return exists; semantic return incomplete | Owner re-observation after success or uncertain effect, Agent acceptance, separate satisfaction |
| O14, O15, O16 | Product owners exist; full shared runtime unproved | Security inventory, applicability, mitigation, and independent verification |
| O17, O18, O24, O25, O26 | Durable handoff and restart mechanisms present; namespace wake defect | Exact live resource resolution and composed lag, restart, broken-wake proof |
| O21 | Structural predecessor lineage present; semantic late-return coverage unproved | Destination-owned old-lineage classification through production |
| O27 | Explicit non-sharing implementation | Compatibility decision, shared node and attempt, two discharge accounts |
| O28 | Pure successor helper only | Live Agent reconstruction, stale eligibility invalidation, current successor judgment and immutable history |
| O29, O30, O31, O32, O33, O34 | Startup remains specified rather than proved | Minimal installed nonce product completes epistemic-executable-epistemic loop and repeats under successor epoch |

## Implementation direction

First close live Agent reconstruction through the existing Planner and Strategy contracts. This is an actual missing feedback transition and removes the driver behavior that treats the first historical authorization as the permanent route. Preserve old products and receipts under their original Plan. Persist the successor decision and completed history, resume after restart, and grant new authority only under the successor cut.

Then connect native Goal judgment and heterogeneous milestone progression to a minimal Startup product, followed by Docs observation and no-action closure. Repair lifecycle evidence ownership and instance-bound wakes as part of making those production traces truthful. Complete compatible Execution sharing and the dissimilar security trace against the same owners. Order may change when running evidence exposes a prerequisite; no outcome is dropped because of that change.

The next connected proof must start from ordinary product installation and reach returned owner evidence through the production Agent path. Native Curation installation is now connected for the shipped Docs package. Ordinary supervisor activation and explicit semantic admission epochs now replace the manual activation prerequisite in the production path. The nonce owner and its Capability must now use the existing neutral Event append and owner-publication seams. Confirmation must name exact Graph visibility and configured realization evidence before Agent acceptance and separate Goal satisfaction. Reopening and changing the admission epoch must preserve old evidence without accepting a predecessor nonce for the new epoch.

These are provisional implementation priorities, not frozen acceptance substitutes. Completion requires the actual connected product behaviors, appropriate restart and adverse-case tests, and removal of superseded runtime authority. A smaller code change that leaves another coordinator or permanently narrows the Plan grammar is not simplification.

## Implemented behavior after the initial audit

The production Agent now selects the admitted leaf of its durable Plan lineage. Changed cut identity invalidates predecessor product eligibility, invokes pure Strategy successor construction, preserves exact completed owner milestones, and atomically records successor admission with predecessor supersession. Historical authorizations no longer choose the current path by hash order. A refused successor grants no predecessor work. An unresolved admitted Execution effect remains attributable and blocks duplicate replacement work.

Curation resolves an unchanged semantic selection to its original frozen operation before Strategy construction. Agent can use a predecessor's exact completed epistemic milestone when that same semantic product remains in the successor. It does not manufacture a new Curation result. Retried Curation intake reuses the canonical Agent currentness and authority check.

Planner exposes a source-material identity for reusable executable products. It retains admitted owner revisions, completeness, material traversal results, decision context, and other semantic sources while excluding transport watermark progress alone. Exact cut identity still fences every new authorization. This prevents unrelated Event progress from turning completed work into another executable obligation. It does not establish cross-admission sharing.

Strategy also constructs and independently verifies a no-work Plan when admitted world-state evidence already establishes the desired condition. Such a Plan has no Task, Epistemic Operation, or prospective execution evidence route. Agent separately records Goal satisfaction only after reassembling the same exact cut and checking live authority. Changed evidence creates a successor and can resume work.

Root no longer fabricates a maintained-condition breach Goal. It passes the installed condition to native Agent. Agent evaluates an exact current cut and records its own condition judgment. An already-established maintained condition creates neither a Goal nor a Plan. Missing knowledge can create a maintenance obligation without claiming an observed breach; an observed mismatch retains actual evaluation provenance. Existing Goal history remains immutable. This proves the shared maintained-condition primitive, not complete Docs observation or repeated transient Goal epochs.

Composed execution exposed a further knowledge defect: a Belief prior could have a satisfactory numeric confidence while its native status remained `NeedsObservation`. Planner now projects the confidence proposition only for settled, fresh knowledge without an open observation obligation. Unsettled evidence remains available through native Belief and Planner provenance. It cannot satisfy the new no-action judgment.

Graph, Belief assessment, evidence ingestion, Curation, Agent, Task admission, Dispatch, and publication now author their semantic lifecycle accounts. Root translates returned accounts and binds structural activation identity. The old root-authored snapshots for these owners and the root-supplied checkpoint arguments are removed. World-model lifecycle transitions exclude concurrent bounded work. Accounts retain actual durable pending operations, publication outboxes, claims, or replay positions rather than assuming an empty work set from a phase label. The legacy Event observer still needs separate ownership and retirement assessment.

Namespace-only wake resolvers are removed. Native resolvers check exact Event ledgers, the durable world-model resource, configured subjects or known operations, and Task Network membership. Native waits carry the bound world-model resource identity without changing semantic product identities. Frozen rules and mappings no longer advertise live revision changes they cannot observe. Root additionally requires the resolver handle's current generation and incarnation. Stopped handles and fenced passive sources cannot supply viable wakes. Composed adversarial checks replace a legitimate wait with foreign ledger, store, and network addresses and require `Stalled`; restoring the native wait restores `Quiescent`.

The composed root test now advances the real Agent, Curation, and Graph owners until convergence, including reopen between steps, rather than requiring a predetermined six-step order. It reaches actual Task admission, production Capability dispatch, failed operational terminal return, and Agent absorption without authorizing duplicate work merely because that outcome advanced the Event stream. Failure remains an operational milestone and does not satisfy the Goal.

Strategy now carries configured prerequisite or confirmation placement and preserves all supplied epistemic products. Agent selects unfinished products by their incoming milestone dependencies rather than by product kind. Completed Task products no longer monopolize the driver. Post-execution confirmation waits for a successor Plan instead of using the pre-execution frozen selection. Native completed history hydrates original product bodies from their immutable source Plans. A confirmation successor retains the completed Task and constructs only the remaining epistemic work. This is a shared progression primitive, not proof of Startup nonce visibility or satisfaction.

Independent verification checks grounded settlement and evidence routes against installed rules, product membership, milestone types, acyclic dependencies, complete Task artifact closure, and grounded operator bodies against the supplied capability vocabulary. A historical dependency requires supplied completed history. Substituting a completed product body from the immediate predecessor fails verification. Current Plans remain independent of predecessor transport positions for their product identities.

Curation can now assess an installed owner-qualified object predicate with exact required qualifications. It authors realization or non-realization within complete declared owner coverage, retains source provenance, and refuses semantic authorship when that coverage is unproved. An expectation alone remains insufficient for realization. A foreign epoch or withdrawn object cannot establish the predicate. Re-observing its own semantic publication returns unchanged state rather than producing an endless series of revisions. Configured realization is optional, preserving the serialized identities of legacy expectation-only rules.

Product receipt projection no longer requires a Docs claim policy for a product that does not select one, and it resolves the explicitly selected observation family among multiple installed family components. The actual Dependency Security package now passes exact receipt resolution with its two installed families and four Capability contracts. This does not claim that both families participate in the live runtime.

Further source inspection found additional end-to-end gaps that accepted preparation did not expose. The native Curation installation gap described in the initial audit is now corrected for packages declaring an epistemic template. Older packages without that component retain their demonstrated active-rule compatibility reader until they migrate. The generic physical/runtime path still assumes one primary observation family. The runtime-subject checkpoint below removes universal workspace and provider bindings. The shipped Security Strategy operators have no asserted effects supporting their settlement obligation, so package resolution alone cannot produce its intended Plan. Docs Capability factories retain a shipped claim policy rather than hydrating an arbitrary selected policy. These are remaining owner integration obligations, not reasons to relax outcome acceptance.

Portable Curation templates now install through `world-model.epistemic-curation-rule.v1`. Curation grounds exact rules for structural Agent assignments during genesis, without setting an active-rule head or starting work. Genesis receipts retain both installed template and grounded rule references. Runtime Curation and Agent resolve those exact references and ask Curation to verify the grounding. An unrelated later active-rule change cannot redirect the prepared product. The shipped Docs package now includes this native component as version `1.1.0`; its CLI runtime proof no longer installs Curation manually.

Package installation and the single-steward product declaration now have one shared root adapter. Docs and Security retain thin package-identity selection wrappers. World initialization selects the observation component, subscription, and authority from the requested package rather than assuming Docs component names. A dissimilar-package test installs Security through this same entrypoint, finds it again with Docs also installed, and verifies its selected posture observation and four exact Capability implementations. This is preparation evidence, not a working Security trace. Its current fixture invoker still returns an empty result and must be replaced by real owner behavior.

The startup-authority defect is now corrected for the composed native path. Agent and Curation factories construct from exact prepared genesis without requiring an `AgentActivationRecord` marked `Activated`. Ordinary supervisor startup opens the canonical lifecycle admission gate. Agent, Curation, Task admission, and dispatch observe the same exact prepared assignment and its current generation and epoch. The shipped Docs CLI and composed startup proofs no longer manufacture an activation record. Legacy activation records do not open this production gate.

Changes extend the existing Agent trees, native store metadata, and owner contracts. There is no new database, background actor, service, coordinator, or Execution planner. The replaced first-authorization selection path is removed. Search still constructs one new executable Task at a time. Broader heterogeneous search, full owner observation return, Startup, sharing, the dissimilar security trace, and remaining legacy authority inventory are unfinished work.


Native authorization now carries an explicit admission epoch in addition to the actual activation generation. Agent binds its Planner context, transient maintained-condition Goal identity, Curation authority, and product authorizations to one observed epoch per tick. It rechecks the frozen authority before granting new work. Closing admission permits no new Plan or product authorization; reopening the same generation creates a distinct epoch and distinct transient Goals. Execution independently checks the epoch at intake and through its dispatch observer. Replaying an already accepted Task preserves the original decision and attribution rather than recalculating historical acceptance under a new gate.

Closed epochs retain their return obligations. Agent archives the exact cut supporting epoch-bound Plans in its existing Plan evidence tree, uses a read-only Execution observation port for predecessor returns, and records their milestones under the original Goal and Plan. Historical return processing cannot authorize dependent products and does not label an archived cut as newly observed current evidence. Native lifecycle evidence includes Goals and pending work from earlier epochs. Curation resumes already accepted work under its original authority, while a queued offer from an obsolete epoch receives an explicit rejection carrying the observed current authority. That rejection prevents obsolete offers from permanently occupying the intake queue.

These proofs establish epoch fencing and attribution, not the complete Startup outcome. A new Goal identity alone cannot prevent a generic Belief about the subject from satisfying an epoch-specific nonce requirement. The minimal product still needs the actual nonce owner, an epoch-specific desired observation, Task emission, exact owner publication and Graph visibility, planned confirmation, configured realization evidence, and separate Agent Goal satisfaction. Recovery and replay must demonstrate that a predecessor nonce cannot satisfy the successor epoch.

The reusable nonce owner is now implemented with its own exact request, deterministic identity, owner publication, hydration, and `nonce.emit.v1` Capability. The request contains an issuer, subject, ordered bounded correlations, opaque fence, and contract revision. Event authority proves the actual append position and canonical body. Repeating the same request under another session resolves the same Event; changing the fence creates a distinct nonce and expected Event. The emitter requires admitted effect authority matching the issuer, subject, and fence. A session identifier cannot supply that authority. Production dispatch carries this context from the admitted Task and its independently checked epoch.

Capability invocation now has an optional neutral durable Event append port bound to the product's exact ledger. The nonce requires that port and returns a receipt from Event authority. It does not rely on progress-context publication, which does not return durable proof. Graph can install an exact owner and Event-kind route for the existing structural publication grammar. The native Graph owner validates and retains that route in existing metadata and includes it in lifecycle evidence. The shipped `meld.nonce` package installs the exact published Capability and native Graph route through the ordinary PDS router without starting work. This route is installed before emission; retroactive backfill for a route added after Graph has consumed earlier Events is not established.

Tracing a complete executable nonce Task exposed another narrowing in the shared path. Task Network already supported frozen initial values, but Task admission and lowering accepted only values produced by another contained step. Strategy could also treat an artifact-existence proposition as input closure without carrying an executable value. Complete Tasks now carry optional exact `TaskInput` values naming their consumer step, Capability slot, artifact type, schema version, and immutable payload. Strategy preserves and verifies the supplied values and includes them in Task identity. Admission validates the exact consumer contract and rejects duplicate, foreign, mismatched, or ambiguous sources. Lowering uses the existing native static-seed mechanism. Artifact existence alone no longer closes an executable Task input. The production Agent's Strategy problem still supplies no epoch-derived nonce input; that remaining integration is not implied by the shared input contract.

The composed nonce proof runs an admitted complete Task through native lowering, dispatch, the production Task executor, and the real nonce Capability. It deliberately interrupts outcome recording after the durable effect. Graph observes the exact nonce publication while Execution still has no terminal outcome. Reopening the durable Task Network and claim artifact repository records one successful outcome and retains one nonce Event with the exact append receipt. This proves the executable and publication portion of the intended trace, including the independent Event-before-outcome case. It does not prove Startup's returned epistemic milestone or Goal satisfaction.

Startup still needs Agent-owned epoch specification, complete bounded negative Event coverage, standing expectation and non-realization, Graph-visibility dependency acceptance, planned confirmation, exact configured realization Belief return, and separate Goal satisfaction. The initial Curation implementation equated its source owner scope with the initiating Agent's subject, branch, and perspective. A nonce belongs to its own source scope while the Agent judges a runtime subject in its own perspective. That source-versus-judgment scope distinction is now an explicit native contract, as described below. Neither a missing nonce row nor a nonce from a predecessor epoch may stand in for the required current evidence. These are implementation obligations under the original mission, not a new product-only slice exclusion.

Curation now supports exact judgment scope independently from its source publication scope. Native template grounding accepts source roots and scope separately from the Agent subject, full perspective key, and branch. Both contexts enter the exact grounded rule identity, and preparation and recovery resolve the installed template and grounded rule without consulting an active head. An epoch-qualified operation publishes its admission epoch in semantic qualifications. Existing assignment-scoped rules preserve their serialized identities when the new field is absent.

A production-port proof emits a real nonce, replays it through native Graph, and runs Curation against actual Traversal with the nonce as source and an independently scoped Agent subject. Curation authors realization, retains epoch and perspective attribution, rejects foreign subjects, perspective kinds, perspective members, and branches, and recognizes its own projected semantic state as unchanged. Reopening the native store resolves the exact source-bound rule and rejects a substituted source. This proves source and judgment separation through the real publication path. The ordinary Agent factory now supplies epoch-specific specification and rule binding. The installed Startup trace is described below.

Changing source rules exposed a separate recovery defect. Curation previously filtered resumable accepted operations by its current configured rule. It now resumes accepted work within the same Agent ownership scope across rule revisions, resolves the original installed rule for that operation, and uses that rule for admission reconstruction and semantic execution. A regression proof installs a successor rule with a different source and expected object, closes new admission, and verifies that the accepted predecessor finishes its original semantic product. New intake remains checked against the current configured rule and authority.

The real source trace also exposes an unproved assumption in the earlier negative-realization mock test. Native Graph reports a missing required owner when no complete publication exists, and native Traversal reports a missing root as an incomplete frontier. The mocked empty result with a complete receipt does not establish a real bounded negative nonce observation. Startup requires explicit owner-declared Event-source coverage through the exact Graph position and a native distinction between proven non-membership and unavailable source material. Coverage evidence must not invent a source Event, and a route installed after earlier Events were consumed cannot claim to have covered them. The emitter's one nonce effect must remain intact while this observation contract is completed.

Native Graph now supports an explicitly selected exhaustive Event source under an exact owner route revision. The nonce owner declares its route exhaustive for its publication scopes. Graph snapshots those routes before a verified replay from ledger genesis and retains that coverage witness in existing native metadata. Cut selection requires that witness, the exact route and owner, and the requested Graph position. A successful replay page that exposes a retention gap is refused without advancing the cursor or establishing coverage. Installing a route after replay has advanced does not grant retroactive coverage.

An Event-source receipt names its actual coverage boundary and route. A positive receipt also names the original source Event. An empty receipt has no source Event and does not fabricate a zero-sequence Event reference. Native Traversal returns explicit absent roots within a complete declared Event-source scope while still walking incoming epistemic references to that absent object. Ordinary unresolved roots remain incomplete. Curation realization targets are explicit traversal roots, preventing an unvisited target elsewhere in the owner scope from being treated as disproved. Published owner-revision inputs retain their existing behavior and wire shape when no Event source is selected.

The real nonce proof now starts with an empty ledger and runs Graph coverage, standing Curation expectation and bounded non-realization, semantic Graph return, one exact nonce emission, and Curation realization after projection. The initial immutable cut continues to prove absence after emission. A distinct epoch's nonce remains absent despite the predecessor nonce. Foreign route revisions, foreign ledgers, projection lag, skipped historical route coverage, and retention gaps do not establish completeness. Curation's returned expectation and realization preserve actual source provenance. No synthetic nonce registration Event or additional emitter effect was introduced.

Proof freshness and material change remain separate. Curation selection, realization identity, and Planner source identity exclude only advancing Event coverage positions over unchanged source contents. Exact cuts still retain those positions for currentness and authorization. The real trace proves that Curation recognizes its returned semantic state as unchanged and stops generating terminal publications. A new nonce publication changes the source material and produces realization.

Historical installation was an explicit implementation obligation at that stage. An exhaustive route added after the canonical Graph cursor has advanced needs bounded native replay of its prior Event history before it can gain coverage. That stage refused the unsupported claim; the native backfill is now implemented below. This is not a final product restriction or a deferred slice exclusion. The installed Startup path now includes epoch-specific Agent specification, Strategy inputs, planned confirmation, configured Belief return, and separate Goal satisfaction. Independent Graph-visibility progression while Execution remains unresolved is still required.

Late owner routes now gain coverage through bounded historical replay inside the existing native Graph actor. Each route has an exact replay identity, a durable checkpoint in existing Graph metadata, and an independent Event consumer report. The main Graph cursor remains unchanged during the historical scan. Matching owner publications pass through the canonical reducer, and every examined record advances the historical checkpoint only after projection writes are durable. Coverage becomes available only after that checkpoint reaches the main Graph position. The route then joins ordinary live replay, and its consumer report follows the main Graph cursor.

The production-port proof installs the nonce route after Graph has already consumed historical Events. One-record ticks process a nonce Event, an unrelated Event, and a second nonce. Reopening the native store and actor resumes at the persisted historical position. The route stays incomplete during partial replay and becomes complete only after all required history is covered. Root reporting translates the native historical checkpoint and counts durable coverage progress without claiming that the main Graph cursor advanced. Native lifecycle evidence includes pending route replay and its durable state. A later nonce follows the normal live route, without duplicate projection or a reset of other Graph state.

A retention-gap proof installs a late route after the required prefix has been pruned. Graph reports the missing historical coverage and retains both its main cursor and the unadvanced route checkpoint. It does not mark the source complete or manufacture a negative observation. This is an honest unavailable-history condition, not permission to reinterpret the remaining suffix as full source coverage.

Native Agent epoch specification is now implemented. The Agent constructs the specification from its canonical persisted genesis, exact maintained condition, runtime subject, and current admission fence before Goal inception. Its native Goal identity provides the non-circular correlation for owner input preparation. Prepared observation selection, exact Curation rule, and frozen Task inputs are retained in the existing Agent reconciliation store. Restart reuses those values, a new epoch obtains a distinct specification, and closed authority cannot prepare new work. Curation validates the selected rule's judgment authority through its public contract.

Agent desired-state evaluation now uses the selected observation subject while Task authority remains attached to the runtime subject. Planner carries the explicit observation subject in its immutable decision context, projects the exact corresponding Belief key, and rejects a projection about a different subject. Existing records omit the new optional field and retain their prior serialized identity. Epoch-aware adapters must explicitly support the selected observation. An unsupported adapter produces an explicit failure and cannot advertise a viable wake or silently substitute its original subject.

The nonce preparation adapter passes Agent-authored opaque correlations to the nonce owner, selects the exact installed nonce operator, and asks Curation to ground the installed template over the nonce's owner-issued source scope. It emits no nonce and authors no satisfaction evidence. The native Agent test carries the frozen input through Strategy into the complete Task and preserves predecessor inputs across reopening and epoch change. The real nonce adapter proof derives distinct nonce requests and Curation expectations for two epochs without appending either nonce. Production Planner ports and native Graph replay then resolve each exact observation selection and report its missing Belief independently from the runtime authority subject.

Standing Curation now reads its exact rule from the persisted Agent epoch specification through a native producer contract. Agent and Curation construction can select that source directly, without a placeholder rule for a nonce that does not yet have an epoch identity. The existing installed-rule path remains available for products whose observation does not change per epoch. Missing epoch preparation waits on the exact Agent store and producer position; a consumer preserves that bound address and rejects a foreign resource. The shared Agent scope function supplies the Goal lookup identity to both producer and consumer.

Accepted Curation operations recover their original installed rule before consulting a new standing selection. Obsolete queued offers can be rejected even when the current epoch has not finished preparation. Native lifecycle evidence distinguishes the producer binding and progress from installed rule identities and cites rules needed by unfinished earlier operations. The composed test now runs the same Curation actor through bounded absence, real nonce emission and Graph visibility, realization, a new epoch awaiting its specification, and bounded non-realization despite the predecessor nonce. The expected objects and returned semantic publications remain distinct.

The ordinary product factory now selects this preparation adapter when the installed maintained condition declares an admission-epoch observation. Native Belief subscription, assessment, and configured realization return are connected to the persisted observation as described below. The production trace now includes planned confirmation, exact configured realization Belief return, and separate Goal satisfaction. A positive standing Belief cannot discard the required confirmation. The runtime-subject checkpoint below removes unnecessary Startup workspace and provider bindings. The Graph-visibility checkpoint now exercises confirmation while the operational return remains unresolved.

Agent now derives each epoch's exact observation subscriptions from its prepared genesis and obtains an opaque acceptance proof from native Belief before advancing reconciliation. Belief validates the complete request identity and installed family contract, retains the accepted request, and indexes pending assessment in its existing subscription tree. Agent retains its separate acceptance receipt. Replaying acceptance after assessment does not schedule duplicate work. Native assessment discovers these observations without a root-supplied subject list, and pending work survives database close and reopen.

The composed nonce path now returns actual Curation result Events through the installed Startup evidence mapping and native Belief ingestion. Unobserved subscriptions produce an explicit observation obligation. Complete bounded absence settles a negative realization Belief. Real nonce emission followed by Graph replay and Curation realization settles a positive successor revision. Planner reads the exact observation and can distinguish all three positions. A new epoch's distinct expectation remains negative despite the predecessor's nonce and positive Belief. This owner-focused test directly emits the nonce. The installed production proof below exercises Execution, planned confirmation, and Goal satisfaction through the ordinary product runtime.

This integration exposed a previously hidden Belief assumption: the shared comparator always blended new observations with the prior and always projected confidence as the complement of the posterior. Those choices are now explicit installed configuration. Existing families retain their serialized defaults. Startup selects the latest observation per schema and direct probability. A later positive or negative observation supersedes its predecessor without requiring repeated confirmation to overcome an old numeric prior. Deterministic evidence selection has separate batch-order and sequential-update proof.

Assessment selection also used a global dirty-key prefix without filtering the full actor scope. Foreign branch or perspective records could consume its budget or be selected under the wrong scope. Native Belief now maintains a secondary scope index over the existing dirty records in existing metadata. Updates and removal are transactional with the primary dirty record. Opening an older store rebuilds that index once. Bounded selection reads only the configured dimension, predicate, policy, complete perspective, and branch. Migration, reopen, foreign-prefix exclusion, and new dirty work have direct tests. This does not establish fairness among multiple busy families or replace the remaining multi-product ingestion audit.

The shipped `meld.startup` package now installs its native maintained condition, Belief family and mapping, Curation template, confirmation Strategy, authority policy, nonce Capability, and exhaustive Graph route through ordinary product compilation. The maintained condition explicitly selects an admission-epoch observation. Genesis retains the template without inventing a source-bound Curation rule before the epoch exists. Production factories select native Agent epoch preparation and subscription, dynamic Curation selection, and Planner assembly from the eventual exact products. Curation's initial lifecycle evidence resolves and cites its actual installed template even before the epoch rule exists.

A composed production test now loads that package, prepares genesis, reopens assembly, opens the canonical lifecycle admission gate, and advances the ordinary supervisor. It emits exactly one nonce through actual Task admission, lowering, and Capability dispatch. The test never calls the nonce emitter. It also advances the Belief registry head before reopening; Agent factory hydration now resolves the exact prepared family rather than following that unrelated head. Reaching this path exposed two scope substitutions hidden by the earlier isolated proofs. Execution chose its operational subject from the reasoning variable naming the expected observation, and the nonce checked its issuer against the authority-granting principal. Complete Tasks now carry an explicit execution subject constructed and verified by Strategy from the runtime decision context. Admission checks that subject against its exact authority decision, and lowering uses it. Execution carries the admitted issuer independently from the granting principal. Nonce checks the issuer, subject, and fence without importing Agent semantics.

The execution-subject field is omitted from old persisted Task bodies. Their demonstrated compatibility lowering remains readable; newly constructed native Tasks carry the explicit subject and bind it into their identity. Existing authority and input-tampering proofs now include a substituted execution subject. The nonce crash/reopen proof uses distinct issuer and granting-principal identities.

Startup now declares `runtime/instance/meld` under `runtime-owner`. Stewardship configuration preserves a complete domain subject separately from optional workspace and provider bindings. Existing string subjects retain their workspace-node compatibility interpretation. A product without a workspace supplies an explicit absolute storage root. Native initialization and live composition retain the same whole assignment subject, and exact owner Capability preparation determines required physical inputs. Dispatch no longer declares a model provider or workspace as universal requirements.

Product compilation derives source participation from the exact installed Curation templates. Startup declares eight participants and no workspace source. Both the native supervisor proof and an ordinary CLI proof run without a workspace directory or provider. The CLI proof loads global configuration, initializes the shipped native package, closes and reopens the product, runs through the ordinary command and dispatch route, and verifies planned confirmation, configured Belief return, separate Goal satisfaction, and durable reopen. No fixture emits a nonce or inserts a completion result. Event authority binding uses the same durable identity and cutover protocol for runtime subjects and existing workspace branches. Runtime products create no workspace branch. Live-status discovery resolves the same physical scope without opening stores.

This exposed obsolete loose-file provisioning before native package installation. World initialization now installs the declared native package directly through its owner routes. It no longer requires Docs-specific filenames or an unrelated claim policy before installing Startup. The standalone legacy provisioning API remains under its existing characterization tests and is still part of the retirement inventory. The shared CLI also retains compatibility node, frame, prompt, and session storage under the product root. Eight semantic participants therefore does not mean all legacy infrastructure has been retired. The Graph-visibility checkpoint below removes the operational-return prerequisite for confirmation. Full epoch restart closure remains required.

The installed Startup proof now reaches separate Goal satisfaction. The ordinary supervisor executes the nonce Task, obtains a new planned Curation request, ingests that request's result through the installed evidence mapping, and admits its configured Belief revision. Agent records Curation terminality and returned Belief as separate milestones. Its Goal disposition cites the accepted milestone identities and the current Planner cut. Extra ticks preserve the same completion history, and reopening assembly retains the exact epoch products, history, and Goal judgment.

This proof exposed three missing behaviors. A positive standing Belief previously removed confirmation from successor construction. Curation could attach a later planned authorization to an earlier standing acceptance. Finally, a completed confirmation could continually generate successors because its own publication changed the source cut. Agent now names the Goal's confirmation request before Plan construction. Curation includes that request in selection and operation identities, so its acceptance cannot be borrowed from standing work. A named observation publishes its own assessment even when the standing semantic value agrees. Replay still reuses the same operation. Strategy carries the prospective evidence return on the epistemic product and preserves completed named requests across their own publication. Independent verification rejects a satisfied successor that skips the required return.

Belief retains the exact interpreted publication record ID in promoted and normalized evidence. Its native source query checks the accepted subscription, complete Belief key, exact family revision, installed mapping revision, evidence schema, and supporting evidence in the requested immutable revision. Only this owner query constructs the opaque return proof. Root does not infer confirmation from generic terminal status. Agent accepts that proof against the original Plan product and then makes its separate Goal judgment. Foreign publication, mapping, and schema substitutions fail the composed proof. Curation terminality alone leaves the confirmation incomplete.

Existing evidence without publication lineage remains readable and cannot establish this new return claim. New publication-linked evidence has a distinct identity, avoiding mutation or conflict with historical records. Older unnamed Curation operations retain their prior identities. The implemented return source currently follows native epoch subscriptions; fixed-rule confirmation still needs an explicit source relationship and reports that missing binding as an error instead of advertising a viable knowledge wake. Changed cuts while planned intake or its return is pending need additional interruption proofs. These are remaining implementation obligations, together with full epoch restart closure, remaining compatibility infrastructure, Docs observation and no-action behavior, compatible Execution sharing, and legacy authority retirement.

## Graph visibility and outstanding Task returns

Startup now installs explicit `graph_confirmation` placement. Native epoch preparation derives an exact publication expectation from the nonce owner's canonical operation. Strategy binds that expectation into the Task identity and declares Graph visibility as the confirmation dependency. The Task retains its separate Execution-terminal return milestone. Legacy prerequisite and terminal-confirmation configurations remain readable with their original omitted-field identities.

Graph owns the visibility proof. Its query validates the frozen cut, re-resolves its owner selection against durable projection, and checks the exact owner, revision, publication scope, canonical Event record identity, and projected Event position. Append without replay, incomplete cuts, foreign publication or scope, and fabricated receipts cannot establish visibility. The opaque proof has no public constructor. Root delegates the query and does not determine visibility from an Event append receipt.

Agent can accept that effect milestone and construct a confirmation successor while its original Task remains unresolved. The original Task remains in lifecycle pending evidence and continues to be observed under its original authorization after Plan supersession and Goal satisfaction. Native historical-return filtering now distinguishes accepted intermediate milestones from completed products. Strategy verification separately rejects operational terminality substituted for required Graph visibility and rejects a changed expected publication identity.

The installed production proof wraps the real claimed invocation at the callback boundary. It lets the nonce Capability execute once, then withholds the callback and leaves recovery unavailable. Execution has a durable claim and no outcome while Graph visibility, planned confirmation, returned Belief, and separate Goal satisfaction complete. Re-enabling callback recovery produces the original Task's terminal return, changes the Agent-owned unresolved-operation evidence, and produces no second nonce. Extra ticks and reopening preserve the completed history and Goal decision. The ordinary CLI proof exercises the normal callback path through the same installed Strategy.

This establishes the tested separation between effect visibility, epistemic confirmation, and operational return. It does not establish full restart closure across successor epochs. At the Graph-confirmation checkpoint, a separate continuous-retry experiment failed: letting every callback retry execute the real Capability again preserves one nonce but repeatedly produces Task lifecycle Events. The last inspected tick reports Curation projection lag at Event sequence 256 versus Graph sequence 254, and Planner repeatedly refuses incomplete traversal. `test-continuous-callback-retry.log` records the executed failure. The experiment temporarily changed only the test wrapper and restored it afterward. That source ended a Graph page after appending derived Events and ran supervisor work in fixed runtime-ID order. The continuous-retry checkpoint below corrects these boundaries while retaining exact currentness.

## Executive review disposition

The [executive review](reconciliation_executive_review_2026_09_06.md) was committed remotely while the planned-confirmation checkpoint was being verified. It was preserved before pushing `6628ea79c1b2f8d9e85ead5616b0a869dc280804`. Its snapshot remains an assessment of the earlier source, not an additional binding gate.

ER-02 is reproduced and corrected by that checkpoint. Extending the installed Startup test first failed because the completion history contained the Task return without planned confirmation. The corrected connected trace requires separate Curation intake, exact configured Belief return, Agent acceptance, and separate Goal satisfaction. An independently verified pure successor cannot discard the confirmation when standing evidence is positive. The connected trace and reopen checks support the correction; the Graph-visibility checkpoint below extends the trace through uncertain Execution return. Full successor-epoch completion remains unfinished.

ER-01 is also reproduced against the production port. An interruption after native Agent authorization but before Execution intake is persisted across closing and reopening the stores. A new workspace owner revision changes the Planner cut while the same Agent, generation, epoch, and policy remain active. Running the former recovery call creates the predecessor Task's first admission and fails the new regression. Corrected Agent reconciliation uses the existing read-only Execution observation method when the Plan cut is stale. An absent admission permits successor construction; it creates no admission, Task node, claim, or outcome. A current undelivered authorization still obtains its first admission through ordinary recovery. An already-admitted predecessor remains observable under its original Plan without duplicate intake. The test injects the interruption through a delegating port and otherwise uses native Agent handles, actual owner publication replay, durable stores, and the production Execution consumer.

The stale-first-admission correction passes 1,861 tests across 52 suites, with zero failures and three ignored, plus strict all-target Clippy, formatting, and diff hygiene. `test-stale-first-admission-before.log` records the executed failure under the former recovery call. `workspace-stale-first-admission.log` and `clippy-stale-first-admission.log` record the corrected source checks in the external task cache.

## Verification and unresolved scope

Baseline world-model library suite: 54 passed, zero failures, run serially on the initial uncommitted source. Passing those tests did not establish the identified missing outcomes.

After live successor implementation, the complete serial workspace library, binary, test, and example suite passed: 1,819 tests, zero failures, three environment-gated tests ignored. Strict all-target Clippy passed with the existing `clippy::result_large_err` allowance at that stage. Native condition, lifecycle, and wake changes subsequently passed 1,824 workspace tests. After dependency progression, successor confirmation, and Curation realization, the complete workspace run passed 1,829 tests across 52 suites, zero failures and three ignored. Strict all-target Clippy also passed. After native Curation installation, the shipped Docs component, and shared package initialization, the complete workspace suite passes: 1,832 tests across 52 suites, zero failures, three ignored. Strict all-target Clippy passes with the existing large-error allowance. Formatting and diff hygiene also pass. Logs are `workspace-shipped-native-curation.log` and `clippy-shipped-native-curation.log` in the external task cache. After ordinary native startup and admission-epoch integration, the complete serial workspace run passes 1,837 tests across 52 suites, with zero failures and three ignored. That run includes the shipped Docs CLI path without manual Agent activation, exact lifecycle epoch observation, distinct native Goals after reopening, predecessor Task return without new authorization, Curation recovery after closure, stale Curation intake rejection, and independent Execution epoch fencing. Strict all-target Clippy, formatting, diff hygiene, and compilation of the affected Execution fuzz target also pass. Current logs are `workspace-admission-epoch.log`, `clippy-admission-epoch.log`, and `check-fuzz-admission-epoch.log` in the same external task cache. After the nonce owner, exact Graph route, durable Capability append, and frozen Task-input integration, the complete serial workspace library, binary, test, and example run passes 1,846 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy, formatting, diff hygiene, and the affected Execution fuzz target compilation pass. Logs are `workspace-nonce.log`, `clippy-nonce.log`, and `check-fuzz-nonce.log` in the external task cache. After source-versus-judgment scope separation and recovery across installed Curation rules, the complete serial workspace run passes 1,848 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy, formatting, and diff hygiene pass. Current logs are `workspace-curation-source.log` and `clippy-curation-source.log` in the external task cache. After native exhaustive Event-source coverage and real bounded negative observation, the complete serial workspace run passes 1,850 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy passes. Strengthened nonce and retention-gap checks also pass after their final test-only additions; formatting and diff hygiene pass. Logs are `workspace-event-coverage.log`, `clippy-event-coverage.log`, `test-event-coverage.log`, and `test-event-coverage-retention.log` in the external task cache. After native historical replay for late owner routes, the complete serial workspace run passes 1,851 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy, formatting, and diff hygiene pass. Logs are `workspace-source-replay.log`, `clippy-source-replay.log`, `test-source-replay.log`, and `test-source-replay-gap.log` in the external task cache. These counts describe bounded regression evidence, not architectural completion.

After native Agent epoch specification, frozen owner inputs, and independent Planner observation selection, the complete serial workspace library, binary, test, and example suite passes: 1,854 tests across 52 suites, zero failures, and three ignored. Strict all-target Clippy, formatting, and diff hygiene pass. The final run includes explicit failure for unsupported observation selection. Logs are `workspace-epoch-spec.log`, `clippy-epoch-spec.log`, `test-epoch-spec.log`, `test-nonce-epoch.log`, and `test-planner-epoch.log` in the external task cache. These checks do not establish ordinary Startup completion.

After native Agent-to-Curation rule selection and removal of placeholder-rule construction, the complete serial workspace suite still passes 1,854 tests across 52 suites, zero failures, and three ignored. The expanded composed test adds live negative and positive realization, a new epoch awaiting preparation, rejection of a foreign resource wake, and non-realization for the new epoch despite the predecessor nonce. Strict all-target Clippy, formatting, and diff hygiene pass. Logs are `workspace-epoch-curation.log`, `clippy-epoch-curation.log`, `test-epoch-curation.log`, and `test-native-epoch-curation.log` in the external task cache. The final test-only iterator correction was verified by the focused composed test and Clippy.

The epoch subscription and Belief-return checkpoint passes 1,857 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy, formatting, and diff hygiene pass. Logs are `workspace-epoch-belief.log`, `clippy-epoch-belief.log`, `test-epoch-belief.log`, `test-latest-observation.log`, and `test-belief-subscriptions.log` in the external task cache. The workspace run includes the final scope-index migration and exact Planner proposition assertions.

The installed Startup emission checkpoint passes 1,859 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy, formatting, diff hygiene, and compilation of the affected Execution fuzz target pass. Logs are `workspace-product-startup.log`, `clippy-product-startup.log`, `check-fuzz-product-startup.log`, `test-product-startup.log`, and `test-docs-execution-subject.log` in the external task cache. Binding the real Event append port in the shared dispatch fixture also requires the Docs return proof to finish bounded Graph replay before asserting Agent terminal absorption. That test now does so and passes.

The planned-confirmation return checkpoint passes 1,860 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy, formatting, and diff hygiene pass. Logs are `workspace-confirmation-return.log`, `clippy-confirmation-return.log`, and `test-startup-confirmation-full.log` in the external task cache. The strengthened Startup proof includes rejection of substituted publication, mapping, and schema references, repeated supervisor ticks, and reopening of its durable products and decisions. These checks establish the demonstrated end-to-end trace, not complete product or architectural coverage.

The host temporary-directory quota prevented a later test invocation from starting. Current logs use `/home/jerkytreats/.cache/meld-reconciliation-20260906`; test temporary files use the task-owned `/var/tmp/meld-reconciliation-20260906` directory. The original baseline backup remains preserved at the initial external location and was also copied there. One parallel world-model test invocation encountered a Sled reopen lock; the serial run passed. Serial execution remains the validation convention.

New tests cover changed knowledge between Curation and Task authorization, immutable completed history, exact successor selection after database close and reopen, refused reconstruction, unresolved Execution effects, transport-only progress versus changed Belief input, already-satisfied Goals without construction recipes, maintained conditions without an initial Goal, stale evidence at satisfaction judgment, unobserved priors, and resumed work after satisfaction becomes invalid. Native lifecycle tests account for actual pending outbox and Curation work. Composed wake tests include foreign resources, malformed positions, stopped handles, and stale incarnations.

Confidence is high for the directly inspected first-authorization driver, narrow Strategy verifier, root breach construction, distinct Execution regions, and lifecycle evidence defects. Product-wide security, all legacy Workflow surfaces, and every crash boundary need further implementation-driven inspection. Native template installation, exact genesis resolution, and generic package initialization now have additional composed proofs. No global completion or exhaustive bug-free claim is made.

Implementation is authorized by the current mission. The program owner subsequently authorized commits and branch pushes at natural verified checkpoints. Deployment remains outside the requested scope. No requested implementation depends on obtaining a new slice authorization. The first checkpoint preserves the accumulated native runtime corrections together with the inherited VC-06 candidate and historical review artifacts. Those artifacts do not certify the current source or restore the program's withdrawn authority.

The runtime-subject checkpoint passes 1,863 tests across 52 suites, with zero failures and three ignored. The complete run includes the ordinary Startup CLI proof and the existing Docs initialization and Event-authority recovery proofs. Logs are `workspace-runtime-subject.log` and `test-startup-cli-no-workspace.log` in the external task cache. The added lock-free runtime-subject description is checked by the focused CLI rerun. Strict all-target Clippy, formatting, and diff hygiene pass. The built `meld` binary also passes `world init`, `runtime run`, and `runtime status` with isolated global configuration and a nonexistent workspace path; `smoke-startup-runtime-subject.log` records that run. `clippy-runtime-subject.log` records static validation. These are bounded execution results and do not close the remaining outcome obligations.

The Graph-confirmation checkpoint passes 1,865 tests across 52 suites, with zero failures and three ignored. The unavailable-callback proof, native lifecycle evidence check, eventual original Task return, nonce idempotency, normal CLI path, and durable reopen are included. Logs are `workspace-graph-confirmation.log`, `test-startup-uncertain-return.log`, `test-startup-graph-final.log`, `test-graph-visibility.log`, and `clippy-graph-confirmation.log`. That checkpoint did not include the continuous-retry experiment in its passing regression count. The correction below makes the experiment a permanent passing regression.

## Continuous retry progress

The previously failing continuous-callback experiment now runs as an ordinary production regression. Every retry executes the real Capability and produces its normal Events while withholding the callback. Startup still accepts exact Graph visibility, completes planned confirmation, receives its configured Belief, and satisfies its Goal while Execution has no terminal outcome. Recovery later absorbs the original Task return and the ledger retains one nonce.

Native Graph now spends the remaining event-record budget on records it derived during the current replay page. The actual derived append position determines whether work remains beyond the durable Graph cursor. A one-record budget stops after that record and reports exhaustion when its derived Event is pending. A larger budget consumes the derived Event in the same invocation. The work lock spans the complete bounded call, preserving native lifecycle safe-point exclusion. Historical source replay retains its independent reporting boundary.

The supervisor rotates the first actor across passes. With a stable active set, every actor gets the first opportunity within one cycle, while each receives exactly one bounded invocation per pass. This removes fixed runtime-ID ordering as a source of starvation without giving root semantic knowledge of the products. Planner and Curation continue to require complete exact cuts. The existing owner actors and durable authorities retain their responsibilities.

The direct proofs cover bounded replay accounting, exact cursor reporting, idempotent derived publication, reopen, and scheduling opportunities. The production stress proof covers the combined liveness behavior. Full successor-epoch completion, the wider Docs and Security outcomes, compatible Execution sharing, and remaining legacy retirement are still required by the mission.

The continuous-retry checkpoint passes 1,868 tests across 52 suites, with zero failures and three ignored. `workspace-continuous-retry.log` records the full run and `clippy-continuous-retry.log` records strict all-target Clippy. `test-continuous-retry-fixed.log` records the initially corrected stress proof. Existing Graph tests now count derived records consumed in the same invocation and assert the resulting durable and reported authority cursors. Formatting and diff hygiene pass.

## Successor epochs and recovery of unreturned effects

The installed Startup proof now completes two consecutive epochs through the ordinary supervisor. Graceful shutdown closes the first generation; reopening and activation prepare a distinct epoch, nonce expectation, observation subject, Goal, and confirmation. Exact Graph visibility, planned Curation, configured Belief return, Execution terminality, and separate Goal satisfaction complete for the successor. The ledger contains exactly two nonce Events. The predecessor's history and Goal decision remain intact, and both epochs' products, histories, and dispositions survive another shutdown and store reopen.

Extending that trace with an unresolved first-epoch callback exposed a source defect. Startup had already accepted the visible effect and satisfied its Goal, but after restart dispatch refused the old Task's closed admission fence on every pass. The new epoch could complete while the original Task remained permanently unresolved. `test-startup-pending-restart-before.log` records the executed failure. This was a missing return path, not permission to execute the old Task again.

Native dispatch now separates effect invocation from recovery of an existing claim. It validates canonical admission attribution before either path. A resumed claim that cannot execute under the current authority may request a read-only completed result from its Capability owner. Unsupported or absent proof preserves the unresolved claim. Fresh stale Tasks still cannot obtain claims, and forged canonical lowering remains rejected. Completed recovered artifacts pass through the existing durable artifact and Task outcome command boundary.

The existing Capability contract now has an optional recovery method. It receives the frozen invocation and historical context plus read-only Event access; it receives no execution API or append capability. The production Task adapter reconstructs ordinary compiled dependencies using this method through the existing native Task runner. It does not invoke effects or emit fresh invocation telemetry. Dynamic expansion recovery remains explicitly unavailable. Owners without recovery support remain unresolved rather than producing invented results. This extension currently proves native nonce recovery, not generic recovery for all Capabilities.

The nonce owner validates the same complete request on invocation and recovery. Recovery asks Events to prove the exact already committed publication and reconstructs the same receipt artifact. Events resolves the canonical record index and verifies the full semantic envelope under its committed watermark. Missing records, uncommitted append positions, a substituted body, and a foreign ledger cannot establish that proof. Reading absent evidence performs no append or flush. No new nonce store, Startup coordinator, Task outcome writer, or lifecycle authority is introduced.

The production interruption variants leave Execution with a durable claim and no outcome while confirmation and Goal satisfaction are already complete. One variant requests real graceful shutdown; the other drops the runtime without shutdown and restarts after the durable owner lease expires. Both recover the predecessor Task under its original identity, retain its prior intermediate milestones, complete the successor epoch, and retain two nonce Events after reopening. Separate negative tests cover missing effect evidence, mismatched nonce fences, foreign ledgers, an uncommitted Event after failed flush, and closed admission without owner proof.

These results establish the tested successor-epoch and uncertain-return paths. They do not close every lifecycle interruption or product mutation scenario. The read-only Startup account remains required, together with changed-cut interruption during planned Curation, fixed-rule confirmation return, Docs observation and no-action closure, compatible shared Execution work, broader Strategy behavior, Security, and the remaining legacy authority retirement. Historical statements above that successor-epoch completion was unproved describe their earlier checkpoints; this section supplies the new bounded evidence without treating Startup as architectural completion.

The successor-epoch recovery checkpoint passes 1,872 tests across 52 suites, with zero failures and three ignored. Strict all-target Clippy, formatting, and diff hygiene pass. Logs are `workspace-startup-restart.log`, `clippy-startup-restart.log`, `test-startup-restart-final.log`, `test-existing-event-proof.log`, `test-dispatch-closed-recovery.log`, and `test-nonce-read-recovery.log` in the external task cache. The full run includes both completed successor epochs, both pending-callback restart variants, and the negative recovery proofs.
