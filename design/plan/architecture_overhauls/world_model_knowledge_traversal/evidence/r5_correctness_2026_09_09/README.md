# R5 correctness evidence

R5 is ready for acceptance against baseline `cd2144b4`, with [clear final neutral review](neutral-review.md). The runtime source identity, binaries, selected theory, actual model, publication and Agent dispositions are recorded in [the proof summary](proof-summary.json). R4 remains accepted. R6 final integrated validation and soft freeze remain open.

The actual local Qwen run left the already-correct [original README](readme-before.md) unchanged. After the [source threshold changed](source-after.py.txt), it published the [repaired README](readme-after.md) once. The complete document is byte-for-byte the original with `8000` replaced by `9000`: its purpose, sections, prose, function signature, error behavior and computation explanation remain intact.

The publication occurred at Event sequence 117. Independent Docs observation at sequence 133 captured the exact published document and changed source, reported complete coverage, and marked the README accepted with supported assertions. Both resulting Agent Goals reached `Satisfied`, at sequences 138 and 142. [Publication and observation records](publication-and-observation.json) retain owner evidence; [compressed native Agent evidence](native-agent-evidence.json.gz) retains the actual dispositions, predecessor Plans, authorizations, cuts, Beliefs and provenance read after shutdown.

Provider requests remained at 21 through settled idle and reopen. README content and modification time also remained unchanged. Reopen added six lifecycle Events and no new provider request or publication. The [model status](model-status.json) identifies actual `qwen3-coder-next` behind provider alias `local`. The run selected 8192 output tokens and a ten-minute whole-owner transport deadline.

The successful run contains one refused validation attempt. The model assigned `non_assertive` to code-line content. The owner rejected that invalid result before any write, and the native successor Plan recovered. This is successful recovery, not flawless first-attempt generation. The strict code-line exclusion remains; local retry efficiency and clearer prompt wording are possible follow-up improvements. Earlier unsuccessful runs and their concrete defects remain in [the failure index](failure-index.json).

The changed-source proof is deliberately bounded. It proves narrative preservation and factual repair for the retained example, not universal readability or arbitrary repository coverage. Installed theory controls authoring and revision style. R5 also corrected the owner representation and evaluation needed to verify assertions within prose, distinguish nonassertive navigation, retain historical identities, and prevent destructive line pruning.

Security V2 now declares its actual supported coverage and independent verification behavior. Its assessment path publishes inventory, advisory, assessment and verification products, plus `dependency_security.product_published.v1`, `dependency_security.invocation_return.v1` and `dependency_security.current_condition.v1`. The mitigation path applies an already-declared, authorized `CodeChangeSet`, publishes `code_change.account_published.v1`, and independently reacquires and verifies dependency state. It does not generate dependency-upgrade proposals. All twelve affected native Security checks pass; these use controlled acquisition fixtures, not an actual Qwen Security run.

[The validation index](validation-index.json) records exact retained log hashes and suite outcomes. Docs, Security, native product paths, affected Context and storage paths, Branch persistence, telemetry and 341 integration cases pass, with three existing integration ignores. Strict workspace all-target Clippy passes. The combined earlier owner log includes a Docs migration failure subsequently corrected and covered by the final 89-case Docs run; its Security sections passed. These are separate bounded commands, not one final workspace regression.

[The retirement scan](retirement.json) supports removal of obsolete aliases and Belief assembly. The program records complete V01 through V18 dispositions, historical-reader removal conditions and intentional source compatibility breaks. Sol delivered bounded implementation and neutral source review; root integrated cross-domain corrections and the composed proof. This allocation worked for R5 and does not establish a universal model requirement.

The durable local evidence workspace is `/home/jerkytreats/.local/state/meld-flywheel-remediation/r5-closeout-20260909`. The successful run is `/home/jerkytreats/.local/state/meld-flywheel-remediation/r5-qwen-20260909T192945Z`. They retain full Events, runtime logs, exact executables, source manifests, installed products, failed attempts and the ordinary CLI driver. Credential configuration is excluded from this evidence directory.
