# Recursive Strategy construction and bounded README work

Slice: `strategy-decomposition-1`. Lifecycle: closed. Readiness: accepted for recursive construction and bounded README work. Baselines: Meld `3f4b89ce`, README `a772f6a`, Codebase Semantics `b00e650`, Eval `50dbc9f` on `feat/strategy-planning`.

The user explicitly authorized implementation of the [Strategy experiment](strategy_planning_experiment.md), with standing checkpoint commit and push authority. This contract selects the first vertical increment. Broader semantic coverage, complete historical causal tracing and the outstanding sensory progress failure remain program obligations rather than implied acceptance of this increment.

Native Strategy must recursively refine existing `StepKind::Goal` method nodes into primitive operations, select applicable alternatives within the existing search bounds, and propagate hypothetical effects to later prerequisites. Plans retain their selected method hierarchy and primitive bindings. Verification checks the refinement and executable graph against the same frozen inputs. The canonical Agent search and successor paths use this behavior.

README supplies authored compound methods and separate candidate drafting and validated publication capabilities. Drafting returns an exact candidate artifact without writing README. Publication consumes that artifact, checks its evidence basis and bounded judgment, and performs the existing atomic write only for an accepted candidate. The old internal draft/review/retry decision loop is removed from the new owner implementation and package. Native Task lineage connects the operations; the harness does not call them directly to manufacture success.

| Responsibility | Mode and current route | Successor and retirement | Proof |
| --- | --- | --- | --- |
| Method refinement | extend native `strategy::search` and `search_successor` through `method_candidates` | One recursive refinement implementation; remove the flat-only method construction block | Nested methods, alternative failure, bounds and causal prerequisites through pure tests and public runtime |
| Plan verification and lineage | extend `StrategyPlan`, exact identity and verifier | Add optional structured refinement with read compatibility for retained Plans; remove initial-state-only checks where predicted causal effects apply | Tampered decomposition, forged operators, missing ordering and old serialized identities |
| README work decisions | replace `readme.repair` orchestration and its package contract | Author method hierarchy and bounded draft/publication offers; remove superseded offer, loop and compiler assumptions | Compiled WAD selects multiple bounded operations and writes the fixture through native Task execution |
| Native inspection | extend existing `agent show --json` products as needed | Reuse canonical Plan serialization, expose retained selected decomposition | Harness captures actual method hierarchy and Task artifacts |
| Events, Graph, Belief, Agent authorization and Execution | retain canonical authorities | No second writer, planner, coordinator or store | Existing regression journeys, independent Goal confirmation, restart |

Runtime writes are bounded to world-model Strategy and its exact Agent/inspection handoffs if required. Domain writes stay in `meld-readme`. Codebase Semantics remains the independent existing publisher for this increment. Harness changes use its artifact-capture API and public Meld commands. Existing language values and native artifact flow are reused. Runtime state stays in the existing external state roots.

The first direct proof runs a composed README WAD against sensed code, captures the actual Plan, checks at least two abstract levels and candidate-to-publication artifact flow, then observes independent Goal satisfaction. Focused tests additionally prove alternative applicability across different frozen states and recursive bounds. Existing CPU shared-consumption and single-Agent runtime journeys remain regression evidence. Provider quality is measured using the pinned Luna-low deployment when a real draft is required.

Source-drift during a long provider call remains a known failing scenario and cannot be presented as fixed by this slice. If recursive planning needs new stores, a separate coordinator, broad language redesign or a second decision authority, stop and reassess. Retained historical packages may remain reproducible artifacts but no newly built owner or package may continue selecting the superseded opaque repair loop.

The checkpoint judgments below supersede the initial pending review posture. The caller migration and retirement boundary includes owner description, grants, compiler, topology, strategy, authority, tests and command probes. Public contract changes must be disclosed before checkpoint commit. This slice may be followed by support-aware reconstruction and runtime progress under the user's program authorization, after its direct proof is accepted.

If applied, this commit makes native Strategy responsible for recursive README task decomposition while preserving bounded domain execution and inspectable lineage.

## Accepted checkpoint and program stop

The [domain closeout](../../../../../meld-readme/evidence/strategy-decomposition-v1/CLOSEOUT.md) and [exact report](../../../../../meld-readme/evidence/strategy-decomposition-v1/report.json) own the composed evidence. Native Luna-low shared maintenance passes 42 checks with two draft calls. CPU shared consumption passes 37 checks. First-use commands pass 22 checks. A normally compiled leaf WAD writes the absent document and reaches independent Goal satisfaction through a three-method native Plan; all ten harness assertions pass.

Logical review accepts the replacement boundary: recursive search uses the existing immutable world-state projection, new method Plans retain their derivation and primitive bindings, verification replays the selected expansion order, and serialized historical Plans without decomposition retain their identity. Ordered predecessors can establish later preconditions. Required artifacts travel through existing Execution contracts. No new runtime semantic authority was added. Conditional method edges remain unsupported, and this slice does not claim general temporal or threat-resolving planning.

Style Assurance passes changed Rust formatting, diff whitespace checks and strict Clippy for world-model, the Meld binary and the README owner. The world-model suite passes 118 tests and the owner passes 16. A final focused test confirms method applicability changes with the same installed method library. Gate Acceptance accepts only this bounded slice. The retained marker and heading failures explain the observed fixes; they are not discarded trials.

Breaking impact was disclosed before commit: the README owner and package replace `readme.repair` with `readme.draft` and `readme.publish`, remove the internal retry budget, and require WAD rebuild plus restart. Native Plan serialization remains backward readable, while Rust struct-literal callers need the added optional field. `agent show --json` adds canonical `authorized_plans` history.

The [new drift proof](../../../../../meld-readme/evidence/strategy-decomposition-v1/DRIFT.md) fails the combined program. Changed-source contribution precedes publication, but semantic admission follows it, so an old complete Graph revision still permits stale output. The experiment stops for architectural reassessment of producer-owned pending-conversion and freshness evidence. Native recursive planning is proved; continuous safe README maintenance and full root qualification are not.
