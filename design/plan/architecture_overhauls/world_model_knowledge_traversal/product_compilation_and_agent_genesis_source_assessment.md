# WMR-VC-05 Product Compilation And Agent Genesis Source Assessment

> Superseded for current reconciliation direction. [Canonical Flywheel Alignment And Runtime Soft Freeze](flywheel_remediation.md) alone owns the issue register, priorities, next work, and branch acceptance. This document retains historical design or evidence; its prior status and authorization statements do not govern current delivery.

Date: 2026-09-05

Status: exact style-successor candidate Gate accepted and slice closed

Source baseline: `a7032bbf`

Authority: direct program-owner authorization for `SI-VC-05`

## Decision

The historical `SI-05` product outcome is reconstructed as `WMR-VC-05`. The active source cut compiles an exact principal-facing product from complete package receipts, creates one situated assignment and finite Agent topology, moves genesis and source subscription acceptance behind their native owners, prepares exact Capability bindings, and persists one inert prepared closure.

The cut does not activate a lifecycle generation. Runtime registration, readiness, wake, replacement, admission epochs, and retirement remain `WMR-VC-06` work.

## Canonical Source Route

```text
principal selection and physical binding
-> exact package installation receipt
-> product declaration revision
-> complete product compilation receipt
-> situated stewardship assignment
-> Agent genesis intent for each declared position
-> durable Agent subscription requests
-> Belief-owned source acceptance
-> canonical Agent genesis Event append
-> Agent genesis publication and receipt bound to the Event position
-> complete Agent topology receipt
-> exact Capability preparation receipt
-> structural participant plan
-> inert prepared activation closure
-> durable prepared product head
```

Root initialization composes this route. PDS owns product identity and complete aggregation. Agent owns genesis acceptance and publication. Belief owns its subscription decision. Capability owns preparation identity. No root adapter may substitute a generated assignment identifier, direct registration, or direct subscription write for these receipts.

## Responsibility Disposition

| Concern | Current entrypoint and incumbent | Successor and caller migration | Storage and exports | Retirement or retention |
| --- | --- | --- | --- | --- |
| principal selection | `StewardshipDeclaration` and physical binding resolve configured intent | world init builds one exact `ProductDeclarationV1` and `StewardshipAssignmentV1` | existing configuration remains the principal adapter | retain as input only; it is no longer product completion evidence |
| package installation | `TheorySelection`, `TheoryRouter`, and package receipts install one package | `ProductCompilationReceiptV1` requires the exact complete receipt set | retain package manifests, router, receipts, and owner registries | retain stable owner installation and exact readers |
| product selection | no complete aggregate existed | `PdsProductStore` persists declaration, compilation, heads, assignment, topology, preparation, and prepared closure | new trees live in the existing theory database | no parallel product store or selector remains |
| Agent registration | world init directly called seed registration | world init submits `AgentGenesisIntentV1`; `AgentGenesis` alone calls registration after lineage validation | Agent store gains intent, request, publication, receipt, and lineage trees | direct root registration is retired; the lower registration API remains Agent-internal |
| source subscription | world init directly called Agent subscription and callers could construct acceptance data | Agent persists a request and Belief mints an opaque proof only after exact durable acceptance | Agent and Belief retain separate durable receipts; the raw Belief writer is crate-private | direct root subscription and forgeable acceptance are retired; lower mutation remains Agent-internal |
| topology completion | one configured Agent and lifecycle label implied completion | `ProductAgentTopologyReceiptV1` requires one exact receipt for every declared position | persisted in `PdsProductStore` | lifecycle labels no longer prove topology genesis |
| Capability preparation | selected contracts and invokers lacked one complete product receipt | `CapabilityPreparationReceiptV1` binds exact contracts, implementations, binding revision, compatibility policy, placement, and limits through activation identity | persisted beside the prepared closure | preparation never claims current availability or realization |
| participant preparation | root could infer a registration set from runtime descriptors | product declaration supplies one exact structural participant plan to the closure | descriptor catalog remains runtime factory inventory | inferred descriptors no longer serve as prepared product evidence; generation projection remains deferred |
| activation identity | root assembly could synthesize compatibility assignment and activation identifiers | assembly rehydrates the exact prepared assignment, activation, topology, and Capability receipt | fake `legacy::` activation lineage is removed | only exact prepared product evidence activates Capability rehydration |
| runtime theory selection | a root receipt current head or a lone package head could independently choose owner semantics | prepared product head resolves the exact closure, compilation, and every named package receipt before any owner body | legacy receipts remain read-only by explicit id with no writer or current selector | live semantic replacement outside a successor prepared product is retired |
| runtime lifecycle | portable lifecycle records and an unused startup writer named overlapping activation state | no lifecycle successor in this cut | the canonical lifecycle service and readers remain unchanged | the duplicate startup writer and store are deleted; all new generation behavior is deferred to `WMR-VC-06` |
| tests and fixtures | several harnesses wrote Agent state directly or requested reduced genesis without a product aggregate | product initialization tests use owner operations; runtime harnesses only load already durable state | focused owner, restart, idempotency, Docs, security, foreign-ledger, fail-closed, and harness proofs are retained | the harness-owned initializer, its public payload, direct test writes, and compatibility genesis expectations are retired |

## Durable Record Families

- PDS product declarations and compilation receipts
- product compilation heads
- situated assignments
- Agent genesis intents and lineage bindings
- Agent subscription requests
- Belief subscription acceptance receipts
- canonical Agent genesis Events, publications, and complete receipts bound to exact ledger positions
- product Agent topology receipts
- Capability preparation receipts
- inert prepared activation closures and prepared heads

All identities are content-derived except monotonic head revisions and observation sequence metadata. Reinstallation reuses immutable content identities. An Agent identifier cannot move to a different assignment lineage.

## Direct Proof

Docs Freshness installs its canonical package, declares one finite steward position, binds its exact directive and Belief family component, completes Agent-owned genesis, persists an inert closure, reruns unchanged, closes and reopens storage, and reconstructs the same topology and Capability preparation.

Dependency Security installs twelve owner-routed components and compiles a separate product declaration with its own steward identity, security directive, observation component, subscription contract, and participant plan. The shared compiler accepts that structure without importing Docs meaning.

## Unchanged Authorities

Graph, Traversal, Belief assessment, Planner, Curation, Strategy, accepted Agent reconciliation, Execution admission, the Task Network, provider dispatch, workspace publication, and Meld Language remain behaviorally unchanged. Events adds the identity-bearing append proof used privately by the Agent-owned publication operation. Belief changes only by adding its native subscription acceptance contract. Capability changes only by adding inert preparation evidence.

## Deferred Boundaries

- lifecycle intent and acceptance
- participant incarnation and readiness
- exact registration projection from the participant plan
- current generation and admission epoch publication
- steady-state wait and wake accounting
- replacement, drain, safe points, stop, and retirement
- Startup nonce proof
- Docs Freshness migration
- Dependency Security migration

## Review State

Three logical reviews failed on 2026-09-05. The second review found that an exact Event proof could come from a foreign authority and that a reduced synthetic genesis route remained production-reachable. The second correction made Event publication an Agent-owned operation over the injected runtime append capability, removed the compatibility body, narrowed the lower mutation surface, and failed closed without complete product inputs.

A focused retirement review then identified surviving noncanonical construction and injection surfaces. Direct user authorization produced the third corrected candidate. It removed the harness-owned initializer, alternate world-init theory installation, caller-supplied semantic world-init content, loose runtime theory injection, the unused startup activation writer, public append-proof minting, and dead lower Agent command surface. It retained the canonical lifecycle service without adding lifecycle behavior.

The third logical review confirmed the prior blockers were closed but found forgeable Belief acceptance and a legacy live runtime theory selector. The fourth corrected candidate introduces the opaque producer-owned Belief proof, confines the raw writer, resolves live theory only through the prepared product compilation, rewires authority policy observation to the same frozen result, removes the legacy writer and current selector, and retires their exclusive characterization surface.

The fourth corrected candidate passed fresh logical review. Its first Style Assurance was superseded by contradictory rustdoc evidence. The exact documentation successor then passed narrow logical confirmation, corrected Style Assurance, and fresh Gate Acceptance in order.
