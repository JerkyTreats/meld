# README PDS: make excellent reasoning maintainable

Date: 2026-09-10
Status: runtime package work authorized; initial owner implementation approach invalidated by the package boundary correction below. No README product qualification is claimed.
Source basis: `cdc13446`, with the README and this requirement authored in the working tree.

## Candidate outcome

Meld 3 must do at least one useful thing well: fully manage a serious README for Meld, with quality appropriate to its code and architecture. The production product is **README PDS**. `docs_freshness` remains a separate internal maintenance and harness PDS. A separate product does not by itself require a new semantic owner.

The user wants strong initial reasoning to remain useful over time. Begin with an excellent document, preserve its editorial intent and evidence relationships, and maintain it through the native runtime. Smaller models should be able to perform routine maintenance with the help of durable knowledge and CPU work. Occasional renewed strong-model judgment is compatible with this outcome. Reliable maintenance and the economic advantage are hypotheses to exercise, not established properties of a model tier.

The [root README](../../../../README.md) is the initial authored baseline for review. It distinguishes implemented mechanisms, demonstrated journeys and product ambitions. It is not yet a compiled README PDS product, an accepted native judgment or evidence of autonomous maintenance.

Acceptance means the document remains useful, accurate, coherent and recognizably itself through representative repository changes. The product must preserve its audience, explanatory structure, working examples and understated voice as well as factual correctness. Mere file existence, supported sentence counts, a successful write or native Goal satisfaction under the old freshness policy cannot establish this outcome.

Public command journeys should show initial assessment, a relevant change and bounded repair, a no-change revisit, and continuation after restart. They should retain the actual model contribution and reuse evidence needed to assess the claim of enduring intelligence. A damaged or flattened document must not pass simply because its remaining claims are supported. These are outcome checks, not a fixed implementation inventory or prescribed benchmark count.

## Why the current core can plausibly hold

The existing loop already separates installed theory, Agent intention, semantic observation, causal planning, authorization, execution and independent Goal judgment. A stronger README definition can follow those same transitions. Authoring richer product theory need not add another planner or teach Execution what makes prose good.

[PDS design](../../../cognitive_architecture/persistent_domain_stewardship.md) explicitly permits semantic resources and satisfaction contracts. [Docs semantic theory](../../../../owners/docs/src/docs/semantics.rs) already retains natural-language instructions for source extraction, README judgment, correspondence, drafting and revision, plus structured response formats. These fields are included in policy and generation identity. A quality standard can contain rich language without adding a corresponding primitive to [meld-lang](../../../cognitive_architecture/meld-lang/README.md).

An Astra-authored standard can therefore be installed product material when authorized by the product principal and admitted through owner contracts. Its authoring model does not become a new runtime authority. Generated prose is not automatically policy: the existing Docs path correctly treats source text and README text as data. Turning an exemplar into editorial instructions requires explicit authored material and admission.

The crucial distinction is between carrying a standard and implementing a meaningful judgment of it. Existing string fields can express editorial intent. They do not automatically extend the maintained condition or establish editorial acceptance.

## What the WAD analogy buys us

The useful interpretation of `meld-struct` is an identifiable body of installed product material that the engine can operate. Keep the name provisional. Current packages, compilation receipts, assignments and native stores already provide much of its physical role.

[Product compilation](../../../../src/theory/product.rs) currently binds exact package receipts and owner revisions. It validates composition; it does not infer an editorial maintenance model from arbitrary Markdown. There is no demonstrated README-to-product compiler in the inspected path.

A proposed README compilation would preserve the document itself alongside an explicit editorial brief, selected obligations, their evidence dependencies and the judgments required when they change. The result must retain information that cannot be reconstructed from the final prose alone. Why a section exists, who needs it and which tradeoffs the author made are part of the valuable initial reasoning.

Immutable selected theory and evolving knowledge remain distinct. The installed product supplies the definition and resources; activation and native stores retain observations, judgments and progress. A portable bundle or snapshot can refer to those records without creating another authoritative state store. An Agent is the maintained intention bound to this material, not the container for every document byte and record.

## Concrete gaps in the present Docs product

| Concern | Current source evidence | Consequence for README PDS |
| --- | --- | --- |
| Document scope | [DocsScopeOperator](../../../../owners/docs/src/docs/scope.rs) selects one document per source directory. The installed package bounds capture to 64 directories and 8192 bytes per source file. | A project introduction needs selected repository-wide evidence without creating a README in every directory. Raising a limit does not express that editorial selection. |
| Editorial preservation | [Current policy](../../../../theory/docs_freshness/claim_policy.docs-claims-strict-v1.json) requests useful prose and minimal repairs, but its drafting instruction limits output to 600 words. | The existing policy can conflict with this substantial baseline. Preserve the internal fixture and define production behavior separately. |
| Acceptance | [Claim policy](../../../../owners/docs/src/docs/claim_validation.rs) evaluates factual claim mass. [Coverage selection](../../../../theory/docs_freshness/epistemic_rule.docs_freshness.json) requires accepted documents and supported correspondence for required claims. | Accuracy and coverage are necessary but do not establish audience fit, coherent explanation or retained voice. Editorial judgment must participate in native satisfaction if it is part of the maintained outcome. |
| Evidence and intent | The current judge grounds factual assertions in captured source and treats README text as reference context rather than truth. | Distinguish implementation facts, declared design intent, product aspirations and editorial preferences. User-authored purpose is not a fact that must be inferred from Rust, and a proposal must not be reported as shipped behavior. |
| Reuse across change | [Source-claim identity](../../../../owners/docs/src/docs/source_claims.rs) binds the selected source collection and policy. Existing source reports can survive README-only changes; changed captured source invalidates that input identity. | Enduring intelligence across code edits needs demonstrated dependency granularity. Current reuse does not prove that one edited function preserves every unaffected judgment. |
| Product refresh | Current preparation preserves genesis lineage and directs changed theory to a separate assignment. | Occasional renewal of the editorial standard is allowed by the outcome, but seamless upgrade of the same Agent is not already demonstrated. |

These are grounded product gaps. They are not evidence that the core flywheel has failed, and no new semantic owner has been justified. The first investigation should locate the smallest owner-owned representation and judgment changes that make this document manageable.

## Joy as editorial intent

The user's phrase, “joy is a property of alignment,” is an editorial hypothesis. Preserve it as part of the brief, with the document as an exemplar. Its observable expression can be reader confidence, a welcoming explanation, useful examples and quiet satisfaction when behavior follows intent. It need not become a keyword requirement, an adjective count or an invented joy score.

CPU work can preserve an unchanged approved passage, check dependencies and maintain the lifecycle of its judgment. When a repair changes the passage's meaning or its place in the document, assessing its voice still requires semantic judgment. A smaller model's ability to make that assessment remains an empirical question. Exact bytes and valid JSON cannot prove aesthetic success.

The initial author should leave the original wording and editorial rationale available to maintenance. Extracting only factual atoms would discard much of the intelligence this proposal intends to preserve. Neither repeated full rewrites nor freezing prose while facts drift satisfies the outcome.

## Next discriminating experiment

Use this substantial README and a controlled change to a documented command or native behavior. Through the external harness, ask whether the existing owners can identify the affected explanation, repair it using a modest model and retain the rest of the document and its evidence. Also challenge quality directly with a factually accurate but flattened revision. Observe whether the proposed production standard can distinguish it from an acceptable maintenance result.

Retain model requests, usage, reused judgments, actual document changes and native completion evidence. A quiet second run is useful but insufficient: changed-source runs must show that preservation is principled, and restart must retain the selected standard. Human review of the baseline and results remains necessary for this first product qualification; a model evaluating its own prose under a permissive policy is not independent evidence of product quality.

This experiment needs a defined production contract before it can be claimed as passing. It does not authorize substituting harness-authored judgments, special-case README scripts or a second maintenance loop for native behavior. It does not require implementing a general prose compiler first.

## Candidate boundary

Command-control delivery remains complete at its recorded scope. This new candidate requirement reopens product acceptance. A complete flywheel is mechanism evidence; the README outcome supplies usefulness evidence. Time since the last master push is a delivery cost to manage, not evidence that either condition has been met.

The existing architectural circuit breaker remains in force. Bounded changes within the current Docs owner and installed theory may support the outcome. If the investigation instead requires fundamental changes to intention, authorization, planning, evidence or satisfaction across the core, stop and reassess before implementation. A serialization container is not a reason to bypass that boundary.

This record accepts the outcome and preserves the hypothesis. It does not select a new language, storage system, compiler architecture, model-routing service or release date. No production README PDS was activated and no model maintenance run or release was performed in this assessment.

## Active slice: installed editorial judgment

Authorization: on 2026-09-10 the user said to proceed, explicitly allowing minor architectural corrections and public API exposure while retaining the deeper architectural circuit breaker. Standing authority permits commits at natural checkpoints. Local validation is authorized; publication and push remain unauthorized.

Baseline: Meld `cdc13446`, corpus `f201efd`. Readiness: build-ready. Maturity: exploratory, with a proved native command journey but no production editorial maintenance evidence.

Use the corpus telemetry-sinks README as the first leaf package. Install its exact editorial brief, exemplar and provenance through the Docs policy route. Extend document assessment to require editorial acceptance whenever that resource is selected. Exercise installation, observation, damage detection, repair, revisit and restart through public commands with isolated state in `meld-eval`. Model output remains a proposed judgment with adapter-authored execution provenance. Synthetic authorship does not establish factual truth.

| Responsibility | Disposition | Owner and proof |
| --- | --- | --- |
| Editorial standard and exemplar | New | Docs admits exact selected policy resources; changed standard changes policy identity |
| Whole-document judgment | Extend | Docs observation and validation share one evaluator; missing or rejected editorial judgment cannot authorize publication or native satisfaction |
| Native reconciliation and publication | Retain | Existing Graph, Curation, Agent, Strategy and Execution consume Docs-owned acceptance without README-specific core logic |
| External experiment | Extend | `meld-eval` supplies workspace mutations and records public evidence; it cannot author native judgments or completion records |

Write scope is the Docs owner, a separate README package and journey in `meld-eval`, and these delivery records. Read scope includes direct native owner, provider, package and runtime contracts. No superseded runtime authority is introduced. Existing freshness policies retain their serialized identity and factual acceptance behavior. Stored JSON reports without editorial material remain readable; newly selected editorial policy cannot accept an absent editorial result.

Tripwires remain changes to core intention, authorization, planning, evidence or satisfaction, a second scheduler or writer, and invented native semantic evidence. General script synthesis and arbitrary data admission remain hypotheses until this package exposes a concrete need. Reusing existing native capabilities is an acceptable first package implementation; it is not evidence of automatic Capability Synthesis.

Logical review, source/style review and composed package acceptance will be recorded against the resulting candidate. A local-model run can establish observed behavior on this leaf, not the smaller-model economics or serious root README outcome. If applied, this commit preserves the accepted README outcome and the first authorized experiment without publishing a release.

### Package boundary correction

The user clarified during implementation: “Thou shalt not have domain theory in my runtime.” The initial attempt added README-specific types and an editorial instruction to the compiled Docs owner under `owners/docs/src/docs`. Although that owner is a separate executable, this placed the new product semantics in the Meld repository. All uncommitted source changes from that attempt were removed. The initial build check failed before any runtime experiment, and no implementation commit or package qualification resulted.

This correction supersedes the active slice's selected implementation location and build-ready status. README domain theory, synthesized data and product-specific implementations belong in an independently authored domain project. The subsequent directory extraction below establishes that location. Meld changes may expose domain-neutral public contracts needed to admit and operate that package. No README-specific type, evaluator or instruction may be introduced into Meld runtime code to make this experiment pass.

The next investigation is the existing external owner contract as a package-owned implementation boundary. The earlier corpus remains experimental domain material. Existing Docs freshness behavior remains intact. If executing the package requires a new core semantic mechanism rather than a bounded public contract correction, flag that gap before implementing it.

### Independent domain directory and external fixtures

On 2026-09-10 the user authorized pulling the domain into its own directory, deferring repository creation, and exploring harness-driven domain development. The domain now lives in [meld-readme](../../../../../meld-readme/README.md), with the complete corpus under `corpus/` and its first leaf scenario under `fixtures/readme_leaf/`. No Git repository was initialized there. The original corpus remains recoverable from `meld-eval` commit `f201efd`.

The move preserves authored entries, source manifest and inspection evidence. Relative navigation was rebuilt for the new location, changing derived Markdown and structure identities. All 199 pairs pass the existing source, reconstruction, child-identity, schema, link, reproducibility and corruption-refusal checks after relocation.

The harness exposes its existing external fixture selection through `--project-root` on list, validate and run. The leaf scenario lives with the domain, including an intact README and a flattened alternative outside the captured input tree. Six harness tests pass, including explicit external selection through the runner and refusal to fall back when the project is absent. The scenario's basic manifest checks are not editorial acceptance.

This is directory separation and a small harness entrypoint extension. No Meld runtime code changed, no independent README package was installed, and no native editorial or maintenance outcome was proved. The current configured runner still assumes the internal Docs product. The next experiment must discover the smallest domain-neutral package/configuration handoff rather than add README meaning to Meld or to the harness. Domain interpretation and synthesized capability implementations stay with the independent project.

### Initial independent PDS checkpoint

The user authorized driving the domain through the harness to a decent initial self-writing README, with Pareto discipline. That bounded prototype is now implemented in [meld-readme](../../../../../meld-readme/README.md). Its [delivery record](../../../../../meld-readme/DELIVERY.md) retains experiments, failures, corrections and limits; [capability status](../../../../../meld-readme/CAPABILITIES.md) maps the broader semantic gaps to current scope.

The package installs an attributed whole-document standard, one external observation participant and one queued repair capability through existing public owner contracts. Native Graph, Curation, Agent, authorization, Execution and lifecycle remain the authorities. No Meld source or public API change was required. The harness handoff for domain-owned configuration and real provider selection is committed in `meld-eval` at `9cb252b`.

The final leaf run `20260910T234008Z-af9161d3` passes missing-document writing, unchanged restart, a new public function, flattened prose and final quiet restart. Each repair uses three provider calls. Both unchanged revisits use none. The substantial project run `20260910T234436Z-e2bb13ed` writes a reviewed 640-word README, reaches native Goal satisfaction with retained authorization, and preserves it on restart with zero new provider calls. The exact generated document was adopted into the domain project after source parity, link and editorial checks.

The model is the user-selected local `qwen3-coder-next`. Five domain tests, strict Clippy, seven harness tests and the existing 199-pair corpus verification pass. Native evidence uses the unchanged Meld binary with SHA256 `5f786de83fbd716ec1ea86e1aa8d311703794dea5a8b1ee2a6a318975a167645`. The [evidence summary](../../../../../meld-readme/evidence/initial-pds.json) binds the external artifacts and run paths.

Early model reviews accepted stale facts and flattened prose. The domain now asks for explicit source understanding and reader usefulness before a verdict, and excludes factual exemplars from review. A package bug also confused a Task-local invocation identity with a global one; native Task and run lineage now scope retained repair records. These were package corrections, not evidence of a failed core flywheel.

This checkpoint qualifies an initial one-document domain prototype. It does not qualify automatic management of the corpus or the serious Meld root README candidate outcome. Model judgments remain fallible; selected source changes invalidate the whole judgment. Fine-grained reuse, parent-child maintenance and active-standard upgrades remain unproved. No continuous service was installed against the live domain directory, no Git repository was initialized there and no release was authorized. No architectural circuit breaker tripped.

### Held-out Meld root maintenance result

The user authorized continuing to the actual Meld root README after the prototype assessment. The frozen independent package accepted the 2104-word baseline against selected current sources. A controlled fixture mutation then renamed only the `runtime follow` resume option from `--after` to `--cursor` in the command declaration and reference guide. The live checkout and executable were not changed.

The first held-out maintenance attempt failed. The actual provider request contained the changed source and stale README; the changed input received a fresh judgment. Coder Next nevertheless accepted the stale command table, so the native runtime performed no repair. A diagnostic restart retained that incorrect judgment with zero further provider calls. This falsifies reliable maintenance for the tested root-document case while preserving the earlier leaf results and external-package integration evidence.

The [held-out evidence](../../../../../meld-readme/evidence/meld-root-heldout.json) records run `20260911T001207Z-ed448313`, frozen package identities, exact observations and the negative editorial result. The baseline also exposed and corrected a harness assumption that every satisfied condition requires a repair Goal and effect authorization. Native already-satisfied conditions now complete through explicit condition evidence and request completion; pending Goals still require their own disposition and authorization. Ten harness tests pass.

This experiment is concluded, with root README product acceptance rejected for the tested case. The core machinery handled the supplied domain judgment consistently; no architectural circuit breaker was triggered. The limiting capability is reliable assessment of specific documented behavior within a substantial document. The next domain iteration needs explicit claim-and-evidence assessment and a different held-out challenge. Further production readiness is not inferred from native satisfaction or zero-call reuse.

### Judgment isolation through the command surface

The user authorized pushing the current branch, then constrained failure isolation to harness and Meld commands. `feat/meld-3-candidate` was pushed at `fefe7b89`. Eight fresh probes retained the compiled owner, runtime, installed standard and provider configuration, varying only copied fixture inputs. Repair dispatch stayed disabled and each probe stopped after a public owner observation Event.

The provider's own response falsely accepts the stale command, and each published owner verdict matches the captured provider verdict. Full-input false acceptance repeats without cached state or output truncation. Shortening either the source or document alone remains insufficient. Narrowing both reveals the specific mismatch once, but the corrected control still invents omissions of behavior explicitly present. Other assessments invent a flag alias, source-file content and supporting parser tests. This isolates unreliable semantic grading; it does not establish that a smaller context alone fixes the package.

The independent domain retains the [experiment matrix and interpretation](../../../../../meld-readme/experiments/judgment/README.md) and [response evidence](../../../../../meld-readme/evidence/judgment-isolation.json). Generic harness commit `96367ab` adds public command capture in retained run contexts, nested JSON selection, recorded copied-workspace overrides, and observation-only runs. Fifteen harness tests pass. All eight native probes pass observation capture and orderly exit; those results are explicitly distinct from editorial acceptance.

No Meld runtime source, domain evaluator or installed policy changed during isolation. Production README qualification remains open. Claim-to-evidence assessment is the next bounded domain experiment, with corrected controls and a different held-out change required. No new core semantic authority or architectural circuit breaker was demonstrated.

### Direct Codex provider comparison

The user authorized implementing a direct Codex provider and repeating the frozen full-document stale and corrected controls with `gpt-5.6-luna` at low reasoning effort. Baseline is Meld `e8d193e2` and harness `96367ab`. At authorization, lifecycle was active and readiness was build-ready. The completed comparison retains exploratory maturity. The target is an observable provider comparison, not a revised README judge or production qualification.

Provider selection and completion are extended through the existing provider factory and completion contract. The new subprocess adapter owns input translation, isolated execution context, deadlines, cancellation, response parsing and execution metadata. Generic completion metadata travels through the existing public Event surface; native intention, judgment, authorization, repair and package authority are retained. Existing HTTP clients retain their behavior. There is no replaced authority or migration of stored state.

Write scope is the provider domain, optional generic execution-response metadata and its construction sites, thin CLI help, harness improvements demonstrated necessary for capture, and independent experiment configuration and evidence. The installed domain owner, instruction, schema and source challenge stay frozen. No T3Code edit, server, new store, automatic model fallback or README semantics in Meld is authorized by this slice. Runtime files stay outside the target workspace under the runtime invariants. Commits at natural checkpoints remain authorized; push and release remain separately controlled.

Proof requires adapter boundary tests, a local compiled binary, public provider selection and captured native stale/corrected judgments through the harness. Record tool use, model selection, input identities, usage and failures rather than interpreting successful transport as editorial success. Logical review, style checks and composed harness acceptance will close this section. A need for a new planner, semantic success authority or core intention model trips the existing circuit breaker. If applied, the implementation checkpoint adds one generic provider transport and preserves the independent README domain boundary.

The implemented candidate adds `provider_type = "codex"`, a noninteractive subprocess adapter, optional generic completion metadata, and public completion/failure execution Events. Provider configuration owns executable and authentication-home selection; request overrides cannot replace them. Existing HTTP clients retain their behavior. The adapter supports the demonstrated system-plus-user structured completion path, bounds output and process lifetime, and refuses unsupported sampling controls, native errors and tool-assisted results. No new service, store, scheduler or semantic authority was introduced. Rust response constructors now need the metadata field, and exhaustive consumers must handle the new provider and error variants; historical serialized responses remain readable.

Native feedback first exposed inadequate failure diagnostics, then a Codex startup error caused by disabling its required Code Mode host. The corrected adapter retains that host and preserves action-tool restrictions. Both failed runs remain in the independent [comparison record](../../../../../meld-readme/experiments/codex/README.md). Generic harness commit `60e0edc` adds stop-on-failure observation probes. A deliberate non-model subprocess failure in `20260911T014055Z-29d7dafe` produces retained public failure evidence, no semantic observation and a failed harness report with orderly runtime exit.

Luna-low rejected the stale README in successful run `20260911T012944Z-e38eb9be`, explicitly identifying the flag mismatch. The final candidate run `20260911T013200Z-73546081` falsely accepted the same supplied stale input. Its corrected control `20260911T013344Z-42b9a05e` was accepted. The final pair shares Meld SHA256 `69a9d443e3a2c13388f26f871163abfec7a8e6e73f82c480a3b5ebcf4f3eccc1`; all successful executions report normal completion and no tool items. The frozen owner and standard are unchanged, and supplied instruction and source hashes match the earlier Qwen experiment. Full assessments and execution identities are retained in [Codex evidence](../../../../../meld-readme/evidence/codex-comparison.json). Codex protocol context differs from the local HTTP provider, and its JSONL reports requested selection rather than independently resolved backend identity.

Logical review is satisfied for provider integration and captured failure behavior. Style assurance is satisfied by changed-Rust formatting, strict Clippy and boundary tests. The full parallel library run recorded 541 passes and 12 failures: eleven executable/store contention errors and one new failure-evidence assertion. All twelve passed isolated sequential reruns without source changes. This retains the parallel-suite caveat rather than claiming one clean full-suite pass. All sixteen harness tests pass. Public provider creation, filtered listing, offline display and removal also pass in isolated state; live provider configuration reads retain the existing store-ownership limitation, while public Events expose active execution.

Gate acceptance is accepted for this bounded provider comparison, with the parallel-test caveat recorded. README semantic acceptance remains rejected: the final paired controls did not discriminate the known defect. The complexity addition is one provider transport, optional generic response/failure evidence and a small harness stop condition, with no replaced authority to retire. The slice closes at the implementation checkpoint; no judge change, repair qualification, production activation, T3Code change, push or release is included. No architectural circuit breaker tripped. Subsequent domain work remains explicit claim-to-evidence assessment, rather than assuming a provider swap establishes reliable maintenance.
